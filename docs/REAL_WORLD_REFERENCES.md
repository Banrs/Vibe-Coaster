# Real-World References — Consolidated Audit

## 1. Purpose

This document is the single, cross-referenced ledger of every project constant that is derived
from (or intentionally deviates from) a real-world roller-coaster record, geometry figure,
physics coefficient, or industry-standard practice. It exists so that any datum in the generator
can be **retraced by an external auditor** back to: the exact value in code, the real-world basis
for that value, the primary source(s) for the real-world figure, the "law" (project rule) that was
applied to turn the real figure into the in-repo constant, and an honest status flag — including
flags for uncited, unverifiable, or internally-conflicting figures. Nothing here launders
uncertainty: where a source audit could not confirm a figure, that is recorded as
`UNVERIFIED_SOURCE` or `NOT_FOUND_IN_CODE`, not silently upgraded to a confirmed record.

This file is a **merge of six prior audit documents** (per-domain research passes, each performed
2026-07-20 against the working tree at `/home/user/Vibe-Coaster`), consolidated here verbatim in
substance — every citation, verdict, and flag from the source audits is preserved. It does not
re-verify their findings; it organizes them for traceability.

**Audit date: 2026-07-20.** All "current record" / "current" language throughout this document is
as of that date; several cited records are explicitly noted as superseded or defunct as of that
date, and each such row says so.

### 1.1 The two project laws under audit

1. **GEOMETRY LAW (dimensional references — heights, radii, drops, lengths, speeds).** The
   reference constant for a geometric element must equal the **real-world RECORD** for that
   element (the most extreme *built* example, cited to a source) — not a "typical" ride. At build
   time, the generator scales that reference into a window of **[1.0x, 1.5x]**
   (`genc::RECORD_SCALE_MIN` / `genc::RECORD_SCALE_CAP`, `v1/gen_constants.h`; **UPDATE
   2026-07-21**: user raised the floor from 0.75x), and the **built population mean must land
   near ~1.25x** (the window midpoint).  Draw centring is **SPEED-AWARE** (user law 2026-07-21):
   the entry-speed percentile maps linearly onto the scale-window position (lower-quartile speed
   -> ~1.125x, upper-quartile -> ~1.375x; `frndUp`, `v1/coaster_track.cpp`), which simultaneously
   enforces the **DURATION LAW**: built mean element duration ~0.75x of `REAL_ELEMENT_SECONDS`
   (`REAL_DURATION_BIAS 0.65-0.85`, centre 0.75) with no element AVERAGING >1.0x real.
   **RATIO LAW (project-wide)**: every real-coaster parameter enters as a DIMENSIONLESS RATIO of
   that coaster's own quantity applied to OUR quantity under the project scalings — never an
   absolute-value transplant.
2. **G-FORCE LAW (per-element felt-g targets).** Per-element felt-g targets = **2x the real-world
   RECORD** for that element type (not 2x a typical/average coaster — records can already run far
   hotter than a normal ride, and the extreme end is the correct anchor). User anchor example:
   Intimidator 305's banked turn sustains ~4 g real -> our banked turns should target ~8 g. All
   g-force targets are additionally hard-gated by the repo's measured **vertical per-sample
   envelope of [-6.5, +12] g** (`v1/coaster_track.cpp:1493` reject in `spatialForceClear`;
   reported/flagged `src/main.cpp:699,711,463`), a **lateral cap of |lat| <= 6.0 g**
   (`genc::LATERAL_G_ENVELOPE`, gated `src/main.cpp:808`), and a **sustained-mean acceptance
   floor** of mean positive >= +9.4 g AND mean negative <= -4.5 g on intense lobes
   (`src/main.cpp:872`, printed target "+10/-5g band +9.4/-4.5" at `:856`) — this floor can reject
   a ride that's too tame but can never clip a high target.

Both laws are project-internal design rules, not physical necessities; they are recorded here as
the yardstick against which every value below is judged (`OK` = law satisfied, `TOO_HIGH`/
`TOO_LOW` = law violated in a stated direction, `INTENTIONAL_DEVIATION` = law knowingly not
applied for a documented reason).

### 1.2 Pointer to future work

**GitHub issue #10** tracks a planned future **3D geometry audit** (rendering/occupancy/support
verification against these same real-world references, as opposed to the closed-form/analytical
checks performed here). This document is the numeric/citation baseline that issue #10's 3D pass
should audit against; it does not itself perform 3D verification.

---

## 2. How to read the tables

Every row uses this column set: **Parameter | Our constant (file:symbol, value) | Real-world
basis (value, coaster, park, year) | Source (name + URL) | Law applied | Status/flags**.

Status/flag vocabulary:
- `OK` — constant satisfies the applicable law within tolerance.
- `TOO_HIGH` / `TOO_LOW` — constant is materially outside the law's target band.
- `UNCITED` — no source of any kind exists in-code or externally for this figure.
- `UNVERIFIED_SOURCE` — a source is claimed (in-code comment or research pass) but could not be
  independently corroborated with a public citation.
- `NOT_FOUND_IN_CODE` — the codebase has no reference constant/citation for an element the law
  says should have one.
- `INTENTIONAL_DEVIATION` — a documented, deliberate departure from the law (e.g. a hardcoded
  1.0x floor instead of 0.75x, explained in-code).
- `PENDING-FIX` — the source audit recommended a specific new value; that value is given.
- `PROJECT_CHOICE` — not a record-derived figure at all; a project engineering/design decision
  (ASTM-derived clearance, water-fraction directive, lap-duration directive). Recorded for
  traceability, not judged against the record laws.

---

## 3. G-FORCES

Per-element **2x-real-world-RECORD** targets (GEOMETRY LAW does not apply here; the G-FORCE LAW
does). Real-world "record" g figures are inherently **methodology-dependent** (accelerometer
placement, sustained-vs-peak window, historic re-analysis) — competing figures are preserved
rather than collapsed to one number.

| Parameter | Our constant (file:symbol, value) | Real-world basis (value, coaster, park, year) | Source (name + URL) | Law applied | Status/flags |
|---|---|---|---|---|---|
| First-drop pull-out | `spatialForceClear(M_DROP,-3.5,11.0)` ceiling, `v1/coaster_track.cpp:850`; `TOPHAT_PULLOUT_FELT_MAX=11.5`, `gen_constants.h:59` | ~4.5 g sustained >6 s — Goliath, Six Flags Magic Mountain (Valencia, CA); ~4.5 g brief — Oblivion pull-out, Alton Towers | ThrillZing/Wikipedia (Goliath, >4.5 g for 6 s); Alton Towers Oblivion pull-out documentation | 2x-record G-FORCE LAW | OK — cap is ~2.2-2.4x record; it is a ceiling, not a forced target |
| High-speed banked turn | `targetPlanG=10.5` -> felt normal ~10.55 g, `v1/coaster_track.cpp:2794` | ~4-4.5 g sustained ("grey-out" 270° turn) — Intimidator 305, Kings Dominion (Doswell, VA); up to 4.8 g in banked helix/turns — Formula Rossa, Ferrari World Abu Dhabi | CoasterForce/CoasterCrazy (I305, ~4-4.5 g, grey-out); Wikipedia — Formula Rossa (up to 4.8 g) | 2x-record; user's own anchor is I305 (8 g) | TOO_HIGH (mild) — 2.2-2.6x record; ~30% hot vs the user's stated 8 g I305 anchor |
| Helix (multi-looper) | `HELIX_TARGET_G=11.75` radial, `gen_constants.h:117`, used `coaster_track.cpp:2952,3029` | 5.5 g measured (3rd loop, normal run), up to ~6.78 g claimed — Mindbender (Schwarzkopf triple-loop); ~4.5 g/6s helix — Goliath/Titan | Wikipedia/Grokipedia — Mindbender (5.5 g measured, ~6.78 g claimed) | 2x-record | OK — right on 2x (1.96-2.1x) |
| Vertical loop peak (bottom) | `invSpec(M_LOOP).gT=10.0`, `coaster_track.cpp:2570`; top capped `gTopCap=4.8`, `:2601` | 5.5-5.9 g (peak ~5.9 g), circular loops — Shock Wave, Six Flags Over Texas; 6.5 g — Moonsault Scramble (defunct); ~12 g historically claimed / ~6-8 g in modern re-analysis — Flip Flap Railway (defunct 1895) | Coasterpedia/RC-Wiki "Highest g-force" (Shock Wave 5.5-5.9 g; Moonsault 6.5 g); GuideToSFOT Shock Wave (peak 5.9 g); Coaster101/Wikipedia — Flip Flap Railway (12 g claimed, ~6-8 g real, historic, non-operating) | 2x-record | TOO_LOW — 10 g vs 2x-modern-record target of ~11-12 g; under-shoots by ~1.5-2 g. **PENDING-FIX**: raise `gT` toward ~11.8 g if targeting 2x the modern operating record (5.9 g); note the +12 g hard cap binds if targeting 2x the historic extreme (see §6 Clearance/Envelope note) |
| Corkscrew / heartline roll | `CORKSCREW_RADIAL_G_REF=8.9`, floor 5.0, `gen_constants.h:93-94` | ~3.85-4 g real (project-cited, not independently sourced in this pass) | project-internal estimate; no external citation traced in this audit pass | 2x-record | OK/TOO_HIGH (mild) — 8.9 g = ~2.3x; inside 2.0-2.3x tolerance |
| Immelmann | `invSpec(M_IMMEL).gT=9.5`, `coaster_track.cpp:2571` | ~4.3 g real peak (B&M half-loop+twist), project-cited | project-internal estimate; no external citation traced in this audit pass | 2x-record | OK — 2.2x |
| Dive loop | `makeDiveLoopPlan targetG=9.6`, `coaster_track.cpp:2341`; `M_DIVE` budget +9.5, `:3359` | ~4.2 g real peak, project-cited | project-internal estimate; no external citation traced in this audit pass | 2x-record | OK/TOO_HIGH (mild) — 2.3x |
| Zero-g stall (sustained neg) | true zero-g ballistic apex ~0 g, `coaster_track.cpp:2273-2290` | ~0 g by definition (weightless hang); strongest hangtime ~-0.5 to -1 g sustained | Coasterpedia — Zero-g stall (near-weightless sustained) | 2x-record (N/A — cannot 2x a ~0 g element) | OK / law N/A — matches real; the law has no meaningful multiplier at ~0 g |
| Airtime hill ejector | crest target ~-5 g (`HILL_ENTRY` -5 g energy solve, `gen_constants.h:176`; refit guard `feltCrest<-5.2`, `coaster_track.cpp:382`); trough +6-7 g, `:2639` | -2.2 g — El Toro (Six Flags Great Adventure, Jackson, NJ) "Rolling Thunder hill"; -2 g — Skyrush (Hersheypark) | CoasterForce (El Toro -2.2 g, Skyrush -2 g) | 2x-record | OK/TOO_HIGH (mild) — -5 g = ~2.3x record; still inside the -6.5 g hard floor |
| Top-hat crest | crest-felt refit guard **-3.8 g** (`beginTopHat`, `coaster_track.cpp` ~`:390`), no longer shared with the hill-crest law; pull-out cap 11.5 g (`TOPHAT_PULLOUT_FELT_MAX`) unchanged | ~0 to -0.5 g at crest (real hyper/strata top hats are a gentle floater, not an ejector) | project-internal characterization; no dedicated external citation traced for "top-hat crest g" specifically | 2x-speed / 1.25x-scale SCALING of the real -0.5 g floater edge | **RESOLVED 2026-07-21** — the guard is now the MATH value, not the -2.2 placeholder: `felt = 1 - (speedRatio^2/scaleRatio)*(1 - feltReal)`; at speedRatio=2, scaleRatio=1.25, feltReal=-0.5 g -> `1 - (4/1.25)*(1.5) = -3.8 g`. The crest curvature falls with scale (~1/scale) while the centripetal term grows with speed^2, so the floater deepens by speedRatio^2/scaleRatio. The +24 m growth cap + reject-on-exceed still bound the exit-kink case; and the free tophat rise draw is deliberately NOT size-biased (biasing it up reintroduced a +13.8 g/-8.7 g exit-junction kink), so the crest/pull-out/exit guards own the size |
| Near-ground dip / turnaround | dip law: crest -2.4 g, valley +7.6 g, `coaster_track.cpp:681-686` | valley ~+3.5-3.8 g, crest ~-1.2 g (project-cited, aggressive dive valley) | project-internal estimate; no external citation traced in this audit pass | 2x-record | OK — textbook exact 2x |

**Envelope-clip analysis (from `gforce_2xwr_audit.md`):** the **+12 g hard cap binds the loop
family**. 2x of the modern loop record (Shock Wave 5.9 g) = 11.8 g (just under the cap); 2x of a
plain 6 g circular loop = 12 g **exactly at the cap**; 2x of the historic extreme (Moonsault 6.5 g
-> 13 g; Flip Flap 6-8 g -> 12-16 g) **exceeds +12 g**. The loop's current `gT=10` sits below the
cap today, but raising it to a true 2x-record target (12 g) would sit exactly on the cap with zero
interpolation margin — and 12 g sustained already exceeds human blackout tolerance, so this is
flagged as a place where the 2x-record law and physical realism collide; it needs a user decision
rather than a silent raise. The **-6.5 g floor has headroom** (2x of the -2.2 g ejector record is
only -4.4 g); it does not clip any current 2x-airtime target. The **+9.4/-4.5 g sustained band is
a floor, not a clip**, and cannot cap a high target.

---

## 4. GEOMETRY REFERENCES — Inversions / Vertical Elements

GEOMETRY LAW audit scope: LOOP, IMMELMANN, DIVE LOOP, CORKSCREW/ROLL, ZERO-G STALL, HELIX, TOP
HAT, first DROP. Repo files: `v1/gen_constants.h`, `v1/coaster_track.cpp`, `src/v1_profiles.h`,
`src/main.cpp`. Note: build/census verification (`cmake -B build --census`) could not be executed
in the audit sandbox — the `raylib` FetchContent download was blocked by the outbound proxy (HTTP
403) — so "mean-bias" rows below marked ANALYTICAL are closed-form projections from the sizing
formula, not measured census output.

| Parameter | Our constant (file:symbol, value) | Real-world basis (value, coaster, park, year) | Source (name + URL) | Law applied | Status/flags |
|---|---|---|---|---|---|
| LOOP height | `genc::LOOP_RECORD_HEIGHT`, `v1/gen_constants.h:60` = 54.5592 m | 179 ft / 54.56 m — Tormenta Rampaging Run, Six Flags Over Texas (Arlington, TX), B&M Dive Coaster, opened **July 9, 2026**; billed at opening as the tallest vertical coaster loop in the world (as of 2026-07-20) | [Amusement Today, Feb 2026](https://amusementtoday.com/2026/02/six-flags-over-texas-completes-worlds-tallest-vertical-coaster-loop-for-tormenta-rampaging-run/); [Fort Worth Report, Jul 8 2026](https://fortworthreport.org/2026/07/08/six-flags-breaks-six-world-records-with-new-ride/); [Coaster101, Sep 2025](https://www.coaster101.com/2025/09/25/tormenta-dive-coaster/) | GEOMETRY LAW (record == reference) | MATCHES_RECORD (exact) — 179 ft × 0.3048 = 54.5592 m to 4 decimal places, exact unit conversion of the current (2026-07-20) park-claimed WR. **Caveat**: Guinness's currently-listed *official* "Largest roller coaster loop" record is smaller (42.52 m/139 ft 6 in), jointly held by Flash (Lewa Adventure, Xianyang, China, opened Jan 2016) and Hyper Coaster (Land of Legends, Antalya, Turkey), verified 13 June 2019 — see [Guinness World Records](https://www.guinnessworldrecords.com/world-records/111031-largest-roller-coaster-loop), [RCDB — Flash](https://rcdb.com/6764.htm); Full Throttle (Six Flags Magic Mountain, opened June 22 2013, 160 ft/48.8 m) was a prior press-release "tallest loop" claim, see [Full Throttle — Wikipedia](https://en.wikipedia.org/wiki/Full_Throttle_(roller_coaster)). Stale in-code comment: `v1/coaster_track.cpp:2023` (`initLoop()`) still says "Full Throttle's 48.8 m loop is the record-height floor" though the constant itself is updated to Tormenta — cosmetic but should be fixed |
| LOOP radius | `genc::LOOP_REFERENCE_CROWN_RADIUS = LOOP_RECORD_HEIGHT * (19.6/48.8)`, `gen_constants.h:62-63` | derived ratio of the record height above | (derived) | GEOMETRY LAW | MATCHES_RECORD (derived); correctly floored/capped via `invRAt`, `coaster_track.cpp:2634-2635` |
| LOOP mean-bias (built population) | Height crosses 1.0x record (54.56 m) only at entry `genV >= 61.2 m/s` (220 km/h); clears the 0.75x floor (40.92 m) at `genV >= 53.0 m/s` (191 km/h) — ANALYTICAL, not measured | n/a (internal consistency check) | n/a | "built mean above 1.0x" clause of GEOMETRY LAW | **PENDING-FIX (instrumentation gap)** — 53-61 m/s is a very hot entry speed for a mid-lap inversion in this generator (cf. `CORKSCREW_ROLL_REF_V=22`, `CORKSCREW_RADIAL_ENTRY_V=34` m/s, `invVMax` cap of 64 m/s only 3 m/s above the record-height crossover); the built LOOP population is analytically more likely to cluster at the 0.75x floor than average above 1.0x, but this is unconfirmed because no `[census] scale LOOP mean=...` probe exists (HELIX is the only element with a live instrumented mean-bias check, `src/main.cpp:1161-1166`) |
| IMMELMANN height | `genc::IMMEL_RECORD_HEIGHT`, `v1/gen_constants.h:61` = 66.4464 m | 218 ft / 66.45 m — Tormenta Rampaging Run, Six Flags Over Texas, opened July 9 2026; billed as "tallest inversion of its kind anywhere" | Same sources as LOOP height above; also [Blooloop](https://blooloop.com/news/tormenta-coaster-open-six-flags) | GEOMETRY LAW | MATCHES_RECORD (exact) — 218 ft × 0.3048 = 66.4464 m exactly |
| IMMELMANN radius | `genc::IMMEL_REFERENCE_RADIUS = IMMEL_RECORD_HEIGHT * 0.5`, `gen_constants.h:64` = 33.223 m | derived (comment `coaster_track.cpp:2586`: "Tormenta Rampaging Run 66.446 m tall -> 33.223 m") | (derived) | GEOMETRY LAW | MATCHES_RECORD (derived); correctly floored/capped, no hardcoded 1.0x floor |
| IMMELMANN mean-bias (built population) | Hitting 1.0x radius (33.223 m) needs `v=52.6 m/s`; clearing 0.75x floor (24.92 m) needs `v>=45.6 m/s` — ANALYTICAL | n/a | n/a | "built mean above 1.0x" clause | **PENDING-FIX (instrumentation gap)** — same structural risk as LOOP; uninstrumented (no `scaleSum`/`scaleN` accumulator for M_IMMEL in `src/main.cpp`) |
| DIVE LOOP drop | `genc::DIVELOOP_RECORD_DROP`, `v1/gen_constants.h:65` = 60.0 m, **no citation comment at all** | No published height/drop figure specifically for the dive-loop *element* was found (as distinct from a dive coaster's overall height/first drop). Yukon Striker (Canada's Wonderland, Vaughan ON, opened May 2019) has 4 inversions of 50 m / 46 m ("zero-g winder") / 37 m ("360° vertical loop") / 31 m (2nd Immelmann) — none labeled "dive loop." Valravn (Cedar Point, opened May 7 2016) is credited as the first dive coaster with a "dive loop" element but no per-element height is published (only the ride's overall first drop, 214 ft/65 m) | [Yukon Striker — Wikipedia](https://en.wikipedia.org/wiki/Yukon_Striker); [Valravn — Wikipedia](https://en.wikipedia.org/wiki/Valravn_(roller_coaster)) | GEOMETRY LAW | NOT_FOUND_IN_CODE (citation) / UNVERIFIABLE (record) — the 60.0 m figure has no in-code citation and no external record was found to confirm/refute it; should get a named-reference comment (HELIX-style) or be flagged "engineering estimate, not a WR." **Also INTENTIONAL_DEVIATION / bug**: `minimumDrop = fmaxf(DIVELOOP_RECORD_DROP, clearance-40.0f)`, `coaster_track.cpp:2352`, uses the bare 1.0x reference as a floor instead of `DIVELOOP_RECORD_DROP * genc::RECORD_SCALE_MIN` (45 m) — a hardcoded 1.0x floor that escaped the Phase-4 0.75x sweep; upper bound is correctly `*RECORD_SCALE_CAP` (`:2353`). **PENDING-FIX**: change floor to `DIVELOOP_RECORD_DROP * genc::RECORD_SCALE_MIN` |
| CORKSCREW / heartline ROLL radius | `genc::CORKSCREW_REFERENCE_RADIUS`, `v1/gen_constants.h:68` = 6.6 m, cited in-code as "inferred from the I.E. Park catalogue's 9.6 m axis / 3 m track elevations" (`coaster_track.cpp:2051-2056`) — explicitly framed as a manufacturer-catalogue reference, not a WR claim | Could not independently verify the "I.E. Park catalogue" figure (no accessible archived Arrow Dynamics sales catalogue found); no formal "largest corkscrew" record is published anywhere (Guinness, RCDB, Coasterpedia) | [RCDB — Corkscrew (Cedar Point)](https://rcdb.com/13.htm); [Corkscrew (Cedar Point) — Wikipedia](https://en.wikipedia.org/wiki/Corkscrew_(Cedar_Point)) (Cedar Point, opened 1976, 620 m total track, first triple-inversion coaster — general context only, not a radius record) | GEOMETRY LAW | UNVERIFIED_SOURCE — self-consistent, honestly labeled as a design-catalogue inference (same posture as HELIX), but not independently corroborable. **Also INTENTIONAL_DEVIATION / bug**: `Clamp(requiredScale, 1.0f, RECORD_SCALE_CAP)`, `coaster_track.cpp:2077`, hardcodes the lower clamp bound to 1.0f instead of `genc::RECORD_SCALE_MIN` (0.75f) — clearest instance of a floor that escaped the Phase-4 sweep; also understates felt radial-g below the speed-trimmed target. **PENDING-FIX**: change `Clamp` lower bound to `genc::RECORD_SCALE_MIN` |
| ZERO-G STALL height/span | **No `genc::` constant exists** — grepped `v1/gen_constants.h` for `STALL_RECORD`/`STALL_REFERENCE`/`ZEROG`/`ZERO_G`: zero matches. `initStall()` (`coaster_track.cpp:2269-2336`) derives everything from hardcoded literals: `h = Clamp(0.030*genV*genV, 16.0, 40.0)` (line 2288), length relation (line 2291), independent of `RECORD_SCALE_MIN`/`RECORD_SCALE_CAP` entirely | No formal world record for zero-g-stall height/span was found; parks market this element by *airtime duration*, not height/length. ArieForce One (Fun Spot America Atlanta, Fayetteville GA, opened April 2023; **park announced permanent closure Aug 2, 2026**) is self-billed as "the largest Zero-G stall in America... nearly four seconds of floating airtime," with no published height/span figure; overall first drop 146 ft/44.5 m at 83° | [Fun Spot America Atlanta — ArieForce One](https://funspotamericaatlanta.com/rides-attractions/arieforce-one/); [ArieForce One — Wikipedia](https://en.wikipedia.org/wiki/ArieForce_One) | GEOMETRY LAW | NOT_FOUND_IN_CODE — starkest violation of the record-anchoring law in the inversion set: no reference constant, no named-ride comment, no RECORD_SCALE term at all. The element's design (duration-first, not height-first) may be a deliberate, defensible exception given the real-world marketing convention has no comparable "record height," but as written it is **undocumented as an exception** (unlike HELIX, which explicitly documents why it deviates) |
| HELIX radius/drop | `genc::HELIX_REFERENCE_RADIUS = 30.5 m`, `genc::HELIX_REFERENCE_DROP = 30.0 m`, `gen_constants.h:108,115-116` — in-code framing (`gen_constants.h:110-114`) explicitly states: "No formal helix-radius world record is published. Six Flags America's engineering workbook does publish 30.5 m for Ride of Steel's first horizontal loop/helix, so this is deliberately named as a real-ride reference rather than a fictitious WR." | Confirmed via search: no public source (Wikipedia, RCDB, Coasterpedia) publishes a "largest helix radius" record for any coaster — the code's own claim that no formal record exists appears correct. The specific "30.5 m, Six Flags America engineering workbook" citation for Superman: Ride of Steel / Wild One (Six Flags America, Upper Marlboro, MD) could **not** be independently verified — only "540°/500°/450° of turn" figures were found, none with a linear radius | [Ride of Steel — Wikipedia](https://en.wikipedia.org/wiki/Ride_of_Steel); [Superman: Ride of Steel — Coasterpedia](https://coasterpedia.net/wiki/Superman:_Ride_of_Steel); [The Wild One — Wikipedia](https://en.wikipedia.org/wiki/The_Wild_One_(roller_coaster)) | GEOMETRY LAW | UNVERIFIED_SOURCE — plausible, honestly labeled as non-WR, but not independently confirmable |
| HELIX_RECORD_REVS | `genc::HELIX_RECORD_REVS = 1.625`, `gen_constants.h:108` — no citation comment anywhere near this constant (isolated from the other HELIX constants at 115-119 by the unrelated Phase-5 corkscrew block) | Source ride/revolution-count claim not found | none traced | GEOMETRY LAW | NOT_FOUND_IN_CODE (citation) — no source at all, not even an internal one |
| HELIX scaling/mean-bias | `initHelix()`, `coaster_track.cpp:2988-3070`, `3170-3181`: `radiusMin/Max`, `dropMin/Max` correctly derived off `HELIX_REFERENCE_RADIUS`/`HELIX_REFERENCE_DROP` × `RECORD_SCALE_MIN`/`CAP`; `dimensionInBand` checks at `3177-3181` | n/a (mechanism check) | n/a | 0.75x/1.5x + "mean above 1.0x" clauses of GEOMETRY LAW | OK, model citizen — correctly wired, no hardcoded 1.0x floors. **This is the ONLY element with a live instrumented mean-bias probe**: `src/main.cpp:1161-1166` accumulates `helixScaleSum`/`helixScaleN` (fed from `coaster_track.cpp:3174`) and prints `[census] scale HELIX mean=%.2fx n=%ld`. Could not execute `--census` in the audit sandbox (raylib FetchContent blocked, proxy 403), so the mechanism is confirmed correctly wired but the **live printed mean was not obtained** — re-run outside the sandbox to close this out. **UPDATE 2026-07-21**: the offer-bias now uses a two-pivot ramp on `HELIX_SCALE_PAR_SPEED` (57 m/s = 1.0x radius) and the new `HELIX_SCALE_125_SPEED` (64 m/s = 1.25x radius, `v^2 = HELIX_TARGET_G*G*(1.25*30.5) - G*30`), up-weighting hot entries so the built-scale MEAN leans toward the SIZING LAW's 1.25x target. **Live census finding**: on the committed HEAD terrain the helix built-scale mean stays ~0.90-0.97x and the offer-weight change is a near-NO-OP — helix is rarely eligible at pick time (clearance/plan-gated), so its weight seldom competes; the mean is set by the (terrain-limited) hot-entry SUPPLY, not the weight. The bias keeps the ORIGINAL 0.6 cold floor because deweighting cold entries harder only STARVES the subtype (a suppressed cold helix is a MISSING helix, not a bigger one — the radius is physics-locked). Reaching 1.25x mean is terrain-bound and cannot be forced without violating the geometry law |
| TOP HAT rise | `genc::TOP_HAT_RECORD_RISE = v1profile::kTopHatReferenceRise = 165.0 m`, `gen_constants.h:42-43`, `src/v1_profiles.h:26`; `TOP_HAT_VERTICAL_CAP = TOP_HAT_RECORD_RISE * RECORD_SCALE_CAP = 247.5 m`, `gen_constants.h:46-47` | 165 m camelback (third-launch camelback hill specifically, not the ride's overall 163 m/535 ft "height to highest point of track" stat — the two figures are close but not identical) — Falcon's Flight, Six Flags Qiddiya City (Al Muzahimiyah, Riyadh Province, Saudi Arabia), Intamin, opened **December 31, 2025** | [BYU Design Review — "The New World's Tallest Roller Coaster"](https://www.designreview.byu.edu/collections/the-new-worlds-tallest-roller-coaster) (165 m camelback / 2,500-ton superstructure); [RCDB — Falcons Flight](https://rcdb.com/21315.htm); [Falcons Flight — Wikipedia](https://en.wikipedia.org/wiki/Falcons_Flight); [Intamin project page](https://www.intamin.com/2026/01/15/six-flags-qiddiya-city-falcons-flight/) | GEOMETRY LAW | MATCHES_RECORD (well-supported, current as of 2026-07-20) — no Guinness-certified "camelback height" category exists, but multiple independent outlets (Intamin, BYU writeup, Coasterforce review) converge on ~163-165 m for this element, and Falcon's Flight is unambiguously the current tallest coaster of any kind |
| TOP HAT face degrees | `genc::TOP_HAT_FACE_DEGREES = kTopHatReferenceFaceDegrees = 64.25°`, `gen_constants.h:44-45`, `v1_profiles.h:27` | Not independently verified. Coasterforce's ride review states Falcon's Flight's steepest drop (a separate 90° cliff-edge drop, not the mid-course camelback) is a full 90° — steeper than 64.25°. No public source quantifies the camelback element's own face angle at 64.25°/60-65° specifically | [Coasterforce — "Riding the World Record Holder"](https://coasterforce.com/falcons-flight-six-flags-qiddiya-city-review/) | GEOMETRY LAW | UNVERIFIED_SOURCE — plausible (camelback faces are typically shallower than a dedicated vertical-drop tower) but not traceable to a citable source |
| TOP HAT scaling bounds | Minor branch: `topHatLo = TOP_HAT_RECORD_RISE*RECORD_SCALE_MIN` (123.75 m, correct), `topHatHi = 195.0f` (hardcoded, not `*RECORD_SCALE_CAP`=247.5), `coaster_track.cpp:357-359`. Major branch: `topHatLo = 235.0f` (hardcoded, not derived from the reference at all), `topHatHi = TOP_HAT_VERTICAL_CAP` (247.5 m, correct), `:357,406` | n/a (mechanism check) | n/a | 0.75x/1.5x clauses of GEOMETRY LAW | INTENTIONAL_DEVIATION (magic-number, not a violation per se) — 235.0/195.0 happen to fall inside the legal [123.75, 247.5] window so nothing is out-of-band, but they are literal constants disconnected from `TOP_HAT_RECORD_RISE`; a future change to the record constant would silently desync them |
| TOP HAT mean-bias (built population) | Minor branch uniform draw range [123.75, 195], raw mean 159.4 m = **0.966x record** (below 1.0x); major branch uniform range [235, 247.5], mean 241.25 m = 1.462x record (above 1.0x, but only reachable when `maxClearH(34)>=235` and a 55% coin-flip hits, `coaster_track.cpp:4738-4741`) — ANALYTICAL | n/a | n/a | "built mean above 1.0x" clause | **PENDING-FIX (instrumentation gap)** — net population mean is untested/uninstrumented (no `[census] scale TOPHAT mean=...` probe exists); a material share of top hats route through the sub-1.0x-centered minor branch, so whether the population mean actually lands above 1.0x is unconfirmed |
| first DROP height | **No reference constant found.** Grepped `v1/coaster_track.cpp` for `DROP_RECORD`/`DROP_REFERENCE`/`firstDrop`/`FIRST_DROP`/`liftHill`/`LIFT_HEIGHT`: no matches. Sizing is purely terrain-driven: `nextHeight = fmaxf(WATER_Y+4, fmaxf(startHeight-250.0f, landing))`, `coaster_track.cpp:763`, bounded by a hardcoded **250.0 m** ceiling, never by RECORD_SCALE against a named constant. Comment at `:783` names Falcon's Flight ("Falcon's Flight's first drop is TWISTED") without anchoring the actual constant | 160.0 m (525 ft) drop from a 195 m/640 ft cliff at 90° — Falcon's Flight, Six Flags Qiddiya City, opened Dec 31 2025 (steepest, tallest single first-drop currently operating). Prior record holders: Kingda Ka, Six Flags Great Adventure (Jackson, NJ), opened May 21 2005, **closed 2024**, 418 ft/127.4 m drop at 90° into a 270° spiral; Top Thrill 2 (rebuild of the 2003 Top Thrill Dragster), Cedar Point, 420 ft/128.0 m drop | [Kingda Ka — Wikipedia](https://en.wikipedia.org/wiki/Kingda_Ka); [Top Thrill 2 — Wikipedia](https://en.wikipedia.org/wiki/Top_Thrill_2); [RCDB — Falcons Flight](https://rcdb.com/21315.htm); [Coasterforce ride review](https://coasterforce.com/falcons-flight-six-flags-qiddiya-city-review/) (90°, 525 ft/158-160 m drop figure) | GEOMETRY LAW | NOT_FOUND_IN_CODE — the code names the record ride in a comment but never anchors the drop height to a `DROP_RECORD_*` constant scaled by RECORD_SCALE_MIN/CAP. The 250 m safety ceiling (250/160 = 1.5625x the real record) is suspiciously close to, but not derived from, the project's 1.5x cap or from `TOP_HAT_RECORD_RISE*RECORD_SCALE_CAP` (247.5 m) either — an independently chosen round-number bound, not a record-scale-contract value |

**Cross-cutting finding (inversions):** the "built mean must land ABOVE 1.0x" law is asserted in
prose (`src/main.cpp:1161-1164`) but only self-checked, via a live instrumented probe, for
**HELIX**. No equivalent `[census] scale <ELEMENT> mean=...` probe exists for LOOP, IMMEL,
DIVELOOP, ROLL, STALL, or TOP HAT — this is itself a finding, not just a per-row gap.

---

## 5. GEOMETRY REFERENCES — Turns / Airtime / Pacing / Misc Geometry

GEOMETRY LAW audit scope: banked TURN, S-CURVE, WAVE, BANKAIR, airtime HILLS, DIP/turnaround,
SPLASH, station LAUNCH, mid-course BOOST, CHAIN lift, tunnel/clearance,
`REAL_ELEMENT_SECONDS`. Files: `v1/gen_constants.h`, `v1/coaster_track.cpp`, plus
`src/ride_constants.h`, `src/game_state.cpp`, `src/v1_profiles.h` (LAUNCH/BOOST/CHAIN constants
are not declared in the two primary audited files).

| Parameter | Our constant (file:symbol, value) | Real-world basis (value, coaster, park, year) | Source (name + URL) | Law applied | Status/flags |
|---|---|---|---|---|---|
| Banked TURN radius (hard) | `gen_constants.h:125 HARD_TURN_REFERENCE_RADIUS=45.0f` (aliased `coaster_track.cpp:47`) | No publicly published turn radius exists for Formula Rossa or any other "biggest banked turn" coaster — parks do not publicize plan-view radii. Fury 325's "190-foot barrel turn" / "101-foot left banked turn" (Carowinds, opened 28 Mar 2015) are **track height** figures, not radius, and not directly comparable | [Formula Rossa — Wikipedia](https://en.wikipedia.org/wiki/Formula_Rossa) (Ferrari World Abu Dhabi, opened 4 Nov 2010); [Fury 325 — Wikipedia](https://en.wikipedia.org/wiki/Fury_325) | GEOMETRY LAW | NOT_FOUND — unverifiable against any published record; zero citation in-code (`gen_constants.h:125-128`), unlike `LOOP_RECORD_HEIGHT`/`IMMEL_RECORD_HEIGHT` a few lines above which name "Tormenta" explicitly. **INTENTIONAL_DEVIATION**: scaling at `coaster_track.cpp:2818-2820` uses a hardcoded 1.0x floor (`Clamp(genV²/(targetPlanG·GRAV), radiusReference, radiusReference·RECORD_SCALE_CAP)`), not the project's 0.75x floor — but this is explicitly documented as deliberate (`coaster_track.cpp:2812-2817`: "the banked-turn family keeps its 1.0x radius floor... the 0.75x sizing win lives in the STARVED families"), so window is [1.0x,1.5x] not [0.75x,1.5x], narrower than but consistent with the law's intent, and the mean is guaranteed >=1.0x by construction |
| Banked TURN radius (speed) | `gen_constants.h:126 SPEED_TURN_REFERENCE_RADIUS=68.0f` | Same as hard-turn row — no published radius exists for any coaster | same as above | GEOMETRY LAW | NOT_FOUND; same documented 1.0x-floor pattern (`coaster_track.cpp:2802-2820`) |
| Banked TURN length (hard/speed) | `gen_constants.h:127-128 HARD_TURN_REFERENCE_LENGTH=210.0f`, `SPEED_TURN_REFERENCE_LENGTH=154.0f` | No published arc length for a specific record turn exists. Internal physics check: 210 m at a plausible 130-160 km/h (36-44 m/s) transit gives 4.8-5.8 s, bracketing `REAL_ELEMENT_SECONDS[M_TURN]=5.5f` almost exactly | (internal consistency only) | GEOMETRY LAW | NOT_FOUND vs external record, but internally self-consistent with the project's own duration table (`dimensionInBand` check at `coaster_track.cpp:2846` allows 0.75x-1.5x, but derived `turnLen` rarely drops under ~1.0x in practice due to the radius floor above) |
| Banked TURN entry/exit shoulder (speed-scaled) | `gen_constants.h TURN_SHOULDER_BASE=0.22f`, `TURN_SHOULDER_MAX=0.48f`, `TURN_SHOULDER_REF_V=44.0f` (used `coaster_track.cpp` initTurn, `turnShoulder(t,frac)`) | Not a ride dimension — an internal transient-comfort derivation, the exact analogue of the corkscrew shoulder row (`CORKSCREW_SHOULDER_*`, §GEOMETRY). A banked TURN's sustained load is the fixed ~84° balance bank of the 9.6 g plan (2x the 4.8 g Formula Rossa record, row above), which is **speed-independent** (radius follows v² by the g-law). The felt-bank frame can only roll in at `ROLL_RATE_DEG_PER_SEC=110` (`ride_constants.h:51`), so the entry-ease length must satisfy `L_ease ≳ φ_bal·v/(ROLL_RATE)` for the governed roll to reach balance; the fixed 0.22 ease violates this above ~44 m/s and leaks felt-lateral (measured `--forceaudit` |lat| 6.15-6.50, and 15.09 with a curvature-jerk spike, at post-launch TURN entries u11/u883 tag4, both seeds). Scaling `frac = clamp(0.22·v/44, 0.22, 0.48)` lengthens the ease with speed exactly like the corkscrew's 5a fix; the curvature **peak** (`turnMag`, the 9.6 g sustained load) is untouched, so no comfort law is weakened | (internal transient-physics derivation; ROLL_RATE governor + fixed φ_bal) — no external ride citation, same posture as `CORKSCREW_SHOULDER_*` | GEOMETRY LAW (transient-comfort, corkscrew-shoulder analogue) | PROJECT_DERIVATION — self-consistent with the ROLL_RATE governor and the 9.6 g plan; REF_V=44 sized so the ease reaches `φ_bal·v/ROLL_RATE` at post-launch speed; MAX=0.48 keeps a curvature peak (2·0.48<1) so the sustained 9.6 g law still peaks. Verified 2026-07-21: `--forceaudit 2` |lat|max 5.19 (seed1)/5.78 (seed2), off tag4 entirely (was 15.09/6.22); TURN family share 60.6% (unchanged vs pre-fix 60.9%), spectrum n=554 capViol=0 |
| S-CURVE radius | `gen_constants.h:129 SCURVE_REFERENCE_RADIUS=SPEED_TURN_REFERENCE_RADIUS=68.0f` | Fury 325 (Carowinds, opened 28 Mar 2015) is widely cited as having "a high-speed S-curve reaching speeds up to 95 mph" (42.5 m/s) — a **speed** citation, not a radius citation; Fury 325's own S-curve radius is not publicly published | [Fury 325 — Wikipedia](https://en.wikipedia.org/wiki/Fury_325) | GEOMETRY LAW | NOT_FOUND for the radius itself; the 68 m value is inherited unmodified from the speed-turn radius, so it inherits that row's NOT_FOUND rather than having an independent S-curve source. **INTENTIONAL_DEVIATION**: `radius=fmaxf(requiredRadius, SCURVE_REFERENCE_RADIUS)`, `coaster_track.cpp:3195` — hardcoded 1.0x floor, same pattern as TURN, but unlike TURN this is **not individually commented/justified**; the admission-gate check (`dimensionInBand`, `:3200`) is looser (0.75x-1.5x) than the draw, so sub-1.0x S-curves are legal but never actually produced. **UPDATE 2026-07-21**: the S-curve PLAN-G target (`makeSCurvePlan`) was corrected from **9.6 -> 6.5 g**. 9.6 was "2x the 4.8 g SUSTAINED banked-turn record", but an S-curve's g-critical point is the UNBANKED reversal crossover (the bank must pass through zero to swap sign), where the rider feels a large FRACTION of the plan lateral unmitigated. Measured crossover/plan ratio ranges 0.72-0.87 across seeds (9.6 plan -> 6.96 crossover; 7.5 -> 6.51), so to hold the worst crossover under the 6.0 g LATERAL_G_ENVELOPE with margin (~5.5 g target): plan-g = 5.5/0.87 = 6.3 -> 6.5 (worst ~5.65 g). Verified: SCURVE is no longer the |lat| peak in `--forceaudit` (it drops to TURN/HELIX at ~6.0) |
| S-CURVE plan length | `gen_constants.h:130 SCURVE_REFERENCE_PLAN=140.0f` | Not independently sourced; scales off S-curve radius row. At Fury 325's cited 95 mph (42.5 m/s), 140 m transits in 3.3 s, but the code's own `REAL_ELEMENT_SECONDS[M_SCURVE]=5.0f` implies a slower ~28 m/s average | (derived from Fury 325 speed citation above) | GEOMETRY LAW + internal duration table | NOT_FOUND externally, with a **flagged internal tension**: geometry reference (scaled off a fast turn) and the duration-bias table imply two different characteristic speeds (~40 m/s vs ~28 m/s) for the same element — SCURVE is the one duration-table row that runs noticeably slower than its one real-speed anchor implies |
| S-CURVE rise | `gen_constants.h:131 SCURVE_REFERENCE_RISE=10.0f` | No public record; S-curve vertical rise is not a publicized coaster spec anywhere | none found | GEOMETRY LAW | NOT_FOUND. Draw floor at `coaster_track.cpp:3220-3222` (`frnd(SCURVE_REFERENCE_RISE, SCURVE_REFERENCE_RISE*scale)`) is exactly 1.0x, consistent with the S-curve family's 1.0x-floor pattern |
| WAVE turn radius | `gen_constants.h:121 WAVE_REFERENCE_RADIUS=100.0f` | Steel Vengeance (Cedar Point, RMC hybrid rebuild of Mean Streak, reopened 5 May 2018) and Zadra (Energylandia, Poland, RMC I-Box, opened 2019) both feature a named "wave turn" element, but no park publishes the wave-turn's plan radius — only the associated hill height is public (see BANKAIR/WAVE hill height row below) | [Steel Vengeance — Wikipedia](https://en.wikipedia.org/wiki/Steel_Vengeance); rcdb.com (Zadra) | GEOMETRY LAW | NOT_FOUND for radius specifically. **INTENTIONAL_DEVIATION**: `requiredRadius=fmaxf(WAVE_REFERENCE_RADIUS, genV²/(3.2·GRAV))`, `coaster_track.cpp:3511-3513` — hardcoded 1.0x floor, same pattern as TURN/S-CURVE, effective window [1.0x,1.5x] |
| BANKAIR / WAVE hill height | `gen_constants.h:67 BANKAIR_RECORD_HEIGHT=35.0f` | "a left-turning, 116-foot (35 m) outward-banked hill" — Steel Vengeance, Cedar Point, opened 5 May 2018. **Exact match**. In-code comment (`coaster_track.cpp:3504`) explicitly cites this: "Steel Vengeance's wave turn is ONE 35 m outward-banked hill" | [Steel Vengeance — Wikipedia](https://en.wikipedia.org/wiki/Steel_Vengeance) | GEOMETRY LAW | MATCHES_RECORD (35.0 m == 35 m cited, exact) — good citation discipline, the one row in the TURN/SCURVE/WAVE family with an in-code named source. **INTENTIONAL_DEVIATION on scaling**: BANKAIR floor `frnd(BANKAIR_RECORD_HEIGHT, fminf(49.0f, affordable))` (`coaster_track.cpp:3473-3474`) — floor is bare 1.0x record, cap `min(49,affordable)` where 49/35=1.4x (below the 1.5x project ceiling); WAVE floor same pattern, cap `min(46,affordable)`, 46/35=1.31x (also below 1.5x). Both hardcoded 1.0x floors, mean guaranteed >=1.0x by construction. **UPDATE 2026-07-21 (SIZING LAW)**: the BANKAIR and WAVE hill-height draws now use `frndUp(lo,hi)` (a single-draw upward-biased triangular, density rising to a mode at hi) instead of uniform `frnd`, shifting the MEAN to `lo + (2/3)(hi-lo)` -- ~1.27x for the [1.0x,1.4x] BANKAIR window and ~1.21x for [1.0x,1.31x] WAVE -- while the full range stays drawable. `frndUp` consumes exactly one rnd01() so it does not perturb the downstream RNG stream |
| BANKAIR reference radius/plan | `gen_constants.h:120 BANKAIR_REFERENCE_RADIUS=132.0f`; local `referencePlan=196.0f` at `coaster_track.cpp:3475` | Not independently published (same gap as WAVE radius). Physics spot-check: 196 m at a plausible 48-55 m/s banked-hill entry speed transits in 3.6-4.1 s, bracketing `REAL_ELEMENT_SECONDS[M_BANKAIR]=4.0f` almost exactly | (internal consistency only) | GEOMETRY LAW | NOT_FOUND externally, internally consistent. Checked via standard `dimensionInBand` (0.75x-1.5x) at `coaster_track.cpp:3486,3492` — no hardcoded floor override found on the *plan* dimension (only height inherits the 1.0x floor) |
| Airtime HILLS height | `gen_constants.h:66 AIRTIME_RECORD_HEIGHT=60.0f` | **No in-code citation at all** — zero comment on this constant, unlike the lines immediately above/below it. Researched real analogs: Fury 325 camelback = 111 ft/34 m (Carowinds, opened 2015); Intimidator 305 second airtime hill = 150 ft/45.7 m (Kings Dominion, Doswell VA, opened 2010); tallest camelback anywhere is Falcon's Flight at 534.8 ft/163 m (Six Flags Qiddiya City, opened 31 Dec 2025) — but that figure is already separately claimed by `TOP_HAT_RECORD_RISE=165.0` for the distinct M_CLIMB "top hat" element, so it cannot double as this element's citation | [Fury 325 — Wikipedia](https://en.wikipedia.org/wiki/Fury_325); [Intimidator 305 — Kings Dominion Wiki](https://kingsdominion.fandom.com/wiki/Intimidator_305); [Falcons Flight — Wikipedia](https://en.wikipedia.org/wiki/Falcons_Flight) | GEOMETRY LAW | UNCITED / NOT_FOUND — 60 m sits in a no-man's-land: ~1.3-1.8x the tallest confirmed real *sustained-airtime* camelback found (34-46 m), but a fraction of the tallest camelback overall (163 m, already claimed elsewhere). This constant needs either a named source or correction to satisfy the "cite it" requirement. Note: scaling itself (floor `AIRTIME_RECORD_HEIGHT*RECORD_SCALE_MIN`, `coaster_track.cpp:574-587`) is the correctly-implemented 0.75x-1.5x window — this is the one exception to the TURN/SCURVE/WAVE 1.0x-floor pattern; only the underlying reference value's citation is missing. **UPDATE 2026-07-21 (SIZING LAW)**: the airtime-hill first-crest-rise draw now uses `frndUp` (single-draw upward-biased) instead of uniform `frnd`, so the built HILLS crest mean leans to ~1.25x of the drawn [min,max] window rather than its midpoint, per the user's 2026-07-21 sizing directive (full range still drawable, one rnd01() consumed) |
| HILLS lobe plan/rail/crown radius | `gen_constants.h:122-124 HILL_REFERENCE_LOBE_PLAN=190.44893f`, `HILL_REFERENCE_LOBE_RAIL=233.50868f`, `HILL_REFERENCE_CROWN_RADIUS=30.625f` | Geometrically derived from the uncited `AIRTIME_RECORD_HEIGHT` (non-round decimal precision is consistent with a computed clothoid/energy solve, not a measured real dimension). `HILL_ENTRY_MIN/MAX` comment (`gen_constants.h:176-180`) documents the -5g energy-solve derivation `v²=g·scale·(2·60+6·30.625)`, confirming these are back-solved from the uncited 60 m height, not a real ride survey | (derived from AIRTIME_RECORD_HEIGHT, above) | GEOMETRY LAW | NOT_FOUND (derived from an uncited parent). **Documented cap-widening exception**: lobe rise uses strict `dimensionInBand` (0.75x-1.5x); lobe plan/rail get a widened upper allowance (`dimensionInBand(lobePlan, HILL_REFERENCE_LOBE_PLAN, 1.25f)` — extra `upperAllowance=1.25` multiplies the cap to 1.5×1.25=1.875x), documented at `coaster_track.cpp:606-614` as intentional ("Allow the flanks a wider upper bound") — a second, smaller, documented departure from the strict 1.5x hard cap, flank-length only, doesn't affect crest g |
| DIP / near-ground turnaround | No `*_RECORD_*`/`*_REFERENCE_*` constant exists for DIP. Purely speed/terrain-derived: `forceLen=1.18·PI·max(genV,40)·sqrt(24/(3·GRAV))/SEG_LEN`, `coaster_track.cpp:3550-3552` | N/A — element is speed/terrain-derived, not geometry-record-anchored | N/A | GEOMETRY LAW (out of scope by design) | NOT_FOUND (by design, not a defect) — there is no geometry reference constant to check against a record; length is `Clamp(max(9,forceLen), 8, 32)` steps (112-448 m), a hard clamp unrelated to the RECORD_SCALE mechanism |
| SPLASH / water dip | No dedicated constant; `dipSplash`/`targetY=WATER_Y+0.9f`, `coaster_track.cpp:3565` | Splashdown "sprays two 60-foot (18 m) lines of water" — SheiKra, Busch Gardens Tampa Bay, B&M dive coaster, opened 2005; "sprays two 50-foot (15 m) plumes" — Griffon, Busch Gardens Williamsburg, B&M, opened 18 May 2007. Neither source publishes splashdown **pool depth**, only spray-plume height | [SheiKra — Wikipedia](https://en.wikipedia.org/wiki/SheiKra); [Griffon — Wikipedia](https://en.wikipedia.org/wiki/Griffon_(roller_coaster)) | GEOMETRY LAW | NOT_FOUND — no geometry reference constant exists to compare; code targets 0.9 m above water surface as a visual/physical submersion depth, unrelated to either cited coaster's (unpublished) pool depth |
| Station LAUNCH top speed | `src/ride_constants.h:43-46 V1_PROPULSION.targetSpeed=360.0f/3.6f` (100 m/s); referenced `coaster_track.cpp:241-242,1048`. **Not in `gen_constants.h`/`coaster_track.cpp`** — lives in a third file as a bare literal, never routed through `RECORD_SCALE_MIN/CAP` | Formula Rossa held the outright top-speed record 2010-31 Dec 2025 at 240 km/h/149.1 mph (Ferrari World Abu Dhabi, opened 4 Nov 2010). **As of this audit (2026-07-20), Formula Rossa is NO LONGER the top-speed record holder** — Falcon's Flight (Six Flags Qiddiya City, opened 31 Dec 2025) now holds it at 250 km/h/155.3 mph. Formula Rossa separately earned a *different* Guinness title in Jan 2026 ("fastest roller coaster launch," 0-240 km/h acceleration in 4.9 s), not top speed | [Formula Rossa — Wikipedia](https://en.wikipedia.org/wiki/Formula_Rossa); [Falcons Flight — Wikipedia](https://en.wikipedia.org/wiki/Falcons_Flight); [Fast Company, Falcon's Flight coverage](https://www.fastcompany.com/91485191/saudi-arabia-falcons-flight-roller-coaster); [Thrillzing — Fastest Roller Coasters 2026 rankings](https://thrillzing.com/roller-coasters/fastest-roller-coasters-in-the-world-2026/) | GEOMETRY LAW (record × 1.5x cap, applied as fixed literal) | TOO_BIG relative to the current record, by design, **and mislabeled**. 360 = 240 × 1.5 (Formula Rossa's **stale, superseded** record × RECORD_SCALE_CAP) — arithmetically clean but built on a record superseded before this audit date. Worse: the in-code comment (`src/ride_constants.h:47-49`) explicitly and **incorrectly** attributes 360 km/h to Falcon's Flight itself ("Falcon's Flight's own top speed arrives once, on its final launch") — but Falcon's Flight's real cited top speed is 250 km/h, not 360. This is a factual misattribution, not just a stale-record issue. **PENDING-FIX**: recite the derivation as "Formula Rossa × 1.5, now stale post-Dec-2025" or rebase onto Falcon's Flight (375 km/h at 1.5x, or 250 km/h at 1.0x) and fix the comment |
| Mid-course BOOST cruise target | `src/ride_constants.h:52 BOOST_CRUISE_TARGET=292.0f/3.6f` (81.1 m/s); referenced `coaster_track.cpp:241`, `src/game_state.cpp:132`. Same third-file caveat as LAUNCH | Same Formula Rossa/Falcon's Flight record pair. 292/240 (Formula Rossa) = 1.217x; 292/250 (Falcon's Flight, current) = 1.168x | Same sources as LAUNCH row | GEOMETRY LAW (deliberately not applied as record×scale) | INTENTIONAL_DEVIATION, honestly reasoned in-comment (`ride_constants.h:47-51`, `game_state.cpp:127-131`): boosts are deliberately capped below the 1.5x launch peak so the following coast arc still clears airtime/inversion entry-speed windows. 292 km/h is a hand-tuned pacing constant, not derived from the RECORD_SCALE formula — coherent as an engineering choice, not coherent as a "record × scale" citation |
| CHAIN lift speed | `src/ride_constants.h:12 CHAIN_V=22.0f` (m/s = 79.2 km/h/49.2 mph); used `src/game_state.cpp:141` (`liftV = tangentY>0.55f ? 27.0f : CHAIN_V`). **Absent from `gen_constants.h`/`coaster_track.cpp`** — not a `*_RECORD_*`/`*_REFERENCE_*` symbol, not routed through RECORD_SCALE | Typical chain-lift speeds run 4-10 mph (1.8-4.5 m/s); the fastest chain lifts in the world reach only ~9 mph (~4.0 m/s). No coaster's chain or cable lift is publicly documented anywhere near 79 km/h | [Lift hill — Wikipedia](https://en.wikipedia.org/wiki/Lift_hill); [CoasterBuzz — Lift Hill Speed forum](https://coasterbuzz.com/Forums/Topic/lift-hill-speed) | GEOMETRY LAW (N/A — not record-derived at all) | **TOO_BIG — by roughly 5x to 12x the fastest real chain lift, not tied to any named record.** Functions as a generic "moderate crawl-to-cruise" gameplay floor, not a chain-lift model; the variable name ("CHAIN_V") actively mis-describes it. No RECORD_SCALE application whatsoever. Should either be renamed to reflect its actual role, or rederived from a cited real chain-lift figure (which would put it at 2-4 m/s, not 22-27) |
| Tunnel / clearance dims | Not referenced anywhere; `grep "tunnel\|Tunnel\|TUNNEL"` over both audited files returns zero matches. Closest generic constants: `gen_constants.h:174 TERRAIN_DECK_CLEARANCE=2.0f`, `gen_constants.h:195 OCCUPANCY_ENVELOPE=6.0f`, neither element-specific or record-anchored | N/A | N/A | GEOMETRY LAW | NOT_FOUND — no tunnel geometry is modeled in the reviewed files; nothing to audit against a real-world tunnel record |
| `REAL_ELEMENT_SECONDS` (TURN/HILLS/SCURVE/DIP/BANKAIR/WAVE) | `gen_constants.h:278-298`: `M_TURN=5.5f`, `M_HILLS=5.0f` (per lobe), `M_SCURVE=5.0f`, `M_DIP=4.0f`, `M_BANKAIR=4.0f`, `M_WAVE=5.0f`. Self-labeled in the table's own header comment (`gen_constants.h:273-277`) as **"clean-room estimate[s]"**, not sourced records; consumed only as a soft lambda bias (`durationBiasedDraw`, `coaster_track.cpp:130-142`), never a hard reject | No external POV-timing citation available (parks/manufacturers don't publish per-element transit times). Internal physics spot-check against the code's own reference geometry at plausible entry speeds: TURN 210m@36-44m/s -> 4.8-5.8s (table 5.5s, consistent); HILLS 233.5m rail@48-66.85m/s -> 3.5-4.9s (table 5.0s, table slightly slower than geometry implies); SCURVE 140m plan@Fury 325's cited 42.5m/s -> 3.3s vs table's 5.0s implying only ~28m/s (table is the slowest of the group relative to its one real-speed anchor); DIP ~168m@genV=50m/s -> 3.4s (table 4.0s, consistent); BANKAIR 196m@48-55m/s -> 3.6-4.1s (table 4.0s, tight match); WAVE 253-288m@50-55m/s -> 4.6-5.2s (table 5.0s, tight match) | (internal consistency only; Fury 325 speed citation as above for SCURVE) | project pacing design, checked for internal consistency | Mostly self-consistent (5 of 6 within ~1s of the geometry-implied transit time). **SCURVE is the outlier** — runs ~1.7s slower than what the one real-speed anchor (Fury 325, 95 mph) implies. Not a hard defect (soft bias only, can't itself violate RECORD_SCALE since it resolves within whatever [lo,hi] the caller passes), but worth a second look given it has an actual real-speed citation to check against |

**Cross-cutting finding (turns/airtime):** the entire banked-curve family — TURN, S-CURVE, WAVE,
BANKAIR/WAVE height — uses a **hardcoded 1.0x floor** rather than the project's 0.75x floor. Only
the TURN case is individually documented as a deliberate Phase-4 choice; S-CURVE and WAVE show the
identical `fmaxf(x, REFERENCE)` pattern with no equivalent per-element comment. The **AIRTIME
HILLS family is the one exception** that correctly implements the 0.75x floor. Net effect: for the
whole TURN/SCURVE/WAVE/BANKAIR group, the built mean is not just "biased above 1.0x" as the law
intends — it is **structurally guaranteed to be >=1.0x**, because the floor of the random draw
itself is 1.0x. **LAUNCH and BOOST are architecturally outside the law's mechanism entirely** —
fixed literals in a third file that never touch `dimensionInBand`/`RECORD_SCALE_MIN/CAP`; this is a
scope gap in the law itself (written for scaled geometry, not fixed powered-propulsion targets),
flagged for the coordinator rather than treated as a violation.

---

## 6. COMPOSITION SHARES

Source: `share_targets_research.md` — a check of whether `SHARE_TARGET` element-frequency
percentages (`gen_constants.h:134` region) are representative, given they were originally derived
from only two record-class coasters (Falcon's Flight and Tormenta: Rampaging Run), tested against
a 13-coaster reference population (gigas/hypers, modern launched/RMC hybrids, classic
multi-loopers).

| Parameter | Our constant (current SHARE_TARGET %) | Real-world basis (reference population finding) | Source (name + URL) | Law applied | Status/flags |
|---|---|---|---|---|---|
| TURN | 14 | Banked turns are universal across the 13-coaster reference population (every coaster has 2-4) | Fury 325 [1], Millennium Force [2], Maverick [9] (see full source list below) | population-representativeness check | OK, mild raise recommended. **PENDING-FIX**: 14 -> 15 |
| SCURVE | 8 | S-curves appear ~1/ride (Fury 325, Maverick), not at an 8%-frequent rate | Fury 325 [1], Maverick [9], Taron [11] | population-representativeness check | TOO_HIGH (mild). **PENDING-FIX**: 8 -> 6; recommend act-multiplier carrier (twisty WATER/CANYON acts), not base |
| DIVE | 4 | Horseshoe/overbanked dive ~1/ride on gigas & dive coasters | Fury 325 [1], Orion [3], Tormenta [14] | population-representativeness check | OK, unchanged (4 -> 4); act-multiplier carrier (WATER/dive acts) |
| WAVE | 4 | Signature but rare (~1/ride: Iron Gwazi, Falcon's Flight) | Iron Gwazi [8], Falcon's Flight [13] | population-representativeness check | OK, unchanged (4 -> 4); act-multiplier carrier (WATER act) |
| HILLS | 15 | **Biggest under-shoot.** Acclaimed modern/giga population is airtime-hill dominated: Fury 325 7 hills, Orion 7, Steel Vengeance ~10-12, Iron Gwazi ~8, El Toro 5-9 | Fury 325 [1], Orion [3], Steel Vengeance [7], Iron Gwazi [8], El Toro [5] | population-representativeness check | TOO_LOW. **PENDING-FIX**: 15 -> 20; recommend act-multiplier carrier (MOUNTAIN/CANYON acts push higher) |
| BANKAIR | 6 | Banked airtime hills common on gigas/RMC (Orion, Iron Gwazi, Falcon's Flight outward-banks) | Orion [3], Iron Gwazi [8], Falcon's Flight [13] | population-representativeness check | OK, modest raise recommended. **PENDING-FIX**: 6 -> 7 |
| DROP | 13 | ~1 primary drop + a recovery/double-down per lap; airtime hills should outweigh drops | Steel Vengeance [7], Iron Gwazi [8] | population-representativeness check | Mild over-weight. **PENDING-FIX**: 13 -> 11 |
| IMMEL | 12 | **Biggest over-shoot — Tormenta artifact.** Tormenta carries 3 Immelmanns (a world-record dive-coaster idiosyncrasy); population norm is 0-1/ride (Montu 1, VelociCoaster 1; every giga and RMC hybrid in the sample = 0) | Montu [12], VelociCoaster [6], Tormenta [14] | population-representativeness check | TOO_HIGH (structural — averaging two opposite-extreme sources, one of which is an outlier). **PENDING-FIX**: 12 -> 4; recommend act-multiplier carrier (CLASSIC/dive-act elevated) |
| LOOP | 4 | Canonical inversion of classic loopers (Kumba 1, Montu 2), rare on hybrids — strongly act-dependent | Kumba [12], Montu [12] | population-representativeness check | Mild under-weight. **PENDING-FIX**: 4 -> 5; act-multiplier carrier (CLASSIC act) |
| ROLL | 2.5 | **Biggest structural fix — most under-weighted inversion.** Neither of the two original sources has one, so the target sits near-zero, but corkscrew/heartline is the **most common real-world inversion** in the reference population: Kumba 3, Steel Vengeance 3, Helix 3, Maverick 2, VelociCoaster 1, Montu 1, Iron Gwazi 1 | Kumba [12], Steel Vengeance [7], Helix (Liseberg) [10], Maverick [9], VelociCoaster [6], Montu [12], Iron Gwazi [8] | population-representativeness check | TOO_LOW (major). **PENDING-FIX**: 2.5 -> 6; recommend base carrier (appears across essentially all classes) |
| DIVELOOP | 3 | Occasional (Kumba, VelociCoaster); no signal from the two original sources but population supports ~3% | Kumba [12], VelociCoaster [6] | population-representativeness check | OK, unchanged (3 -> 3); act-multiplier carrier (CLASSIC act) |
| STALL | 3 | Zero-g stall is a signature of nearly every acclaimed modern hybrid (VelociCoaster, Steel Vengeance, Iron Gwazi, Falcon's Flight) | VelociCoaster [6], Steel Vengeance [7], Iron Gwazi [8], Falcon's Flight [13] | population-representativeness check | Mild under-weight. **PENDING-FIX**: 3 -> 4; recommend base carrier |
| HELIX | 4 | Giga/terrain signature, ~1/ride (Fury 325, Millennium Force, Orion, El Toro, Helix) | Fury 325 [1], Millennium Force [2], Orion [3], El Toro [5], Helix (Liseberg) [10] | population-representativeness check | OK, unchanged (4 -> 4) — matches by coincidence, not because the two original sources featured it (neither did); act-multiplier carrier (MOUNTAIN/terrain acts) |
| DIP | 3 | Near-ground/water dip is a WATER-theme signature | (population-general, no single-coaster anchor cited) | population-representativeness check | OK, unchanged (3 -> 3); act-multiplier carrier (WATER act) |

**Revised sum:** 15+6+4+4+20+7+11+4+5+6+3+4+4+3 = **96** (baseline was 95.5; ~4-point headroom
intentionally reserved for count-ruled features — top hat 1/lap, splash finale). **Inversion-budget
check:** IMMEL 4 + LOOP 5 + ROLL 6 + DIVELOOP 3 + STALL 4 = **22%** (was 24.5%), consistent with
the project's hard 4-inversions/lap budget; mix now mirrors the reference population (**ROLL >
LOOP ~ STALL > IMMEL ~ DIVELOOP**) instead of being Immelmann-dominated. **Banked-family gate:**
revised TURN+SCURVE+DIVE = 15+6+4 = **25** (was 26) — suggest relaxing the gate to ~25 to match, or
keep 26 and let the +1 headroom absorb WAVE.

**Overall verdict from the source research:** the population-representativeness hypothesis is
*partly* right. Falcon's Flight is actually a good general representative (airtime/giga-school
layout, zero inversions, matches the wider gigas/hypers class). **Tormenta is the outlier**: as the
world's first "giga dive coaster" it carries an anomalous triple-Immelmann + record vertical loop,
and averaging it 50/50 with Falcon's Flight inflates IMMEL and starves ROLL and HILLS. The
recommended fix is to **rebalance, not rebuild**, and to move the high-variance rows (HILLS, HELIX,
IMMEL, LOOP, DIVELOOP, DIVE, WAVE, DIP, SCURVE) onto ACT theme multipliers rather than the shared
base table.

**Sources consulted (numbered, referenced above):**
1. Fury 325 — [Wikipedia](https://en.wikipedia.org/wiki/Fury_325); [Coasterpedia](https://coasterpedia.net/wiki/Fury_325); [Carowinds fact sheet](http://www.multivu.com/players/English/7256251-carowinds-.../7256251-fury-325-fact-sheet.pdf)
2. Millennium Force — [Wikipedia](https://en.wikipedia.org/wiki/Millennium_Force); [Coasterpedia](https://coasterpedia.net/wiki/Millennium_Force_(Cedar_Point))
3. Orion — [Wikipedia](https://en.wikipedia.org/wiki/Orion_(roller_coaster)); [ThrillZing](https://thrillzing.com/roller-coasters/orion-in-motion/)
4. Formula Rossa — [Wikipedia](https://en.wikipedia.org/wiki/Formula_Rossa)
5. El Toro — [Wikipedia](https://en.wikipedia.org/wiki/El_Toro_(Six_Flags_Great_Adventure))
6. VelociCoaster — [Wikipedia](https://en.wikipedia.org/wiki/VelociCoaster); [Coasterpedia](https://coasterpedia.net/wiki/Jurassic_World_VelociCoaster); [ThrillZing](https://thrillzing.com/roller-coasters/velocicoaster-universal/)
7. Steel Vengeance — [Wikipedia](https://en.wikipedia.org/wiki/Steel_Vengeance); [Coasterpedia](https://coasterpedia.net/wiki/Steel_Vengeance)
8. Iron Gwazi — [Wikipedia](https://en.wikipedia.org/wiki/Iron_Gwazi); [RCDB](https://rcdb.com/16985.htm); [ThrillZing](https://thrillzing.com/roller-coasters/iron-gwazi-busch-gardens-tampa/)
9. Maverick — [Wikipedia](https://en.wikipedia.org/wiki/Maverick_(roller_coaster)); [Coasterpedia](https://coasterpedia.net/wiki/Maverick)
10. Helix (Liseberg) — [Wikipedia](https://en.wikipedia.org/wiki/Helix_(roller_coaster)); [Coasterpedia](https://coasterpedia.net/wiki/Helix_(Liseberg))
11. Taron — [Ultimate Rollercoaster](https://www.ultimaterollercoaster.com/coasters/taron); [blooloop](https://blooloop.com/theme-park/news/intamin-supply-record-breaking-taron-lsm-multi-launch-coaster-to-phantasialand/)
12. Kumba & Montu — [Kumba — Wikipedia](https://en.wikipedia.org/wiki/Kumba_(roller_coaster)); [Montu — Wikipedia](https://en.wikipedia.org/wiki/Montu_(roller_coaster)); [Busch Gardens](https://buschgardens.com/tampa/roller-coasters/kumba/)
13. Falcon's Flight — [Wikipedia](https://en.wikipedia.org/wiki/Falcons_Flight); [RCDB](https://rcdb.com/21315.htm); [Coaster101 review, Jul 2026](https://www.coaster101.com/2026/07/07/falcons-flight-review-six-flags-qiddiya/)
14. Tormenta Rampaging Run — [Wikipedia](https://en.wikipedia.org/wiki/Tormenta_Rampaging_Run); [ThrillZing](https://thrillzing.com/roller-coasters/tormenta-rampaging-run-inside-the-worlds-first-giga-dive-coaster-at-six-flags-over-texas/); [RCDB](https://rcdb.com/22243.htm); [Guide to SFOT](https://guidetosfot.com/tormenta-rampaging-run-first-impressions-ride-review/)

---

## 7. PHYSICS CONSTANTS

Source: `physics_realism_memo.md`. Model: `coastAcceleration(speed, tangentY) = -GRAV*tangentY -
DRAG*speed^2 - FRICTION` (`src/game_state.cpp:119-121`), a single-body model applying quadratic
aerodynamic drag plus a constant rolling-resistance term uniformly regardless of grade or bank
angle. `FRICTION` is never multiplied by `GRAV` anywhere in the codebase (confirmed by grep). Note:
`FRICTION`/`DRAG` provenance in the header comment (`ride_constants.h:8-9`) is stated as
"calibrated against the 64-seed live integrator" to hit "240 km/h" — a **speed-gate-driven
rationale**, which the project rule explicitly prohibits as a basis for these constants; this memo
evaluates both purely against physical literature, independent of the speed target.

| Parameter | Our constant (file:symbol, value) | Real-world basis (value/derivation) | Source (name + URL) | Law applied | Status/flags |
|---|---|---|---|---|---|
| `DRAG` (aerodynamic) | `src/ride_constants.h:10 DRAG=0.00040f` (specific drag, `a_drag=DRAG·v²`) | Physically, `DRAG=½ρC_dA/m`: ρ=1.225 kg/m³ (sea-level air density); C_d≈0.8-1.2 for an open-topped coaster train (comparable to a passenger-train consist); frontal area A≈3-5 m²; laden train mass estimated 3,000-9,500 kg (no manufacturer publishes a laden mass for a record-class 14-seat train — Falcon's Flight itself doesn't disclose one; bracketed by scaling public per-seat figures: ~535 kg empty + ~100 kg/rider per car, and a 36-seat Millennium Force train at ~27 tons/~750 kg per seat-equivalent). Combined envelope: DRAG_low=0.000155, DRAG_mid=0.000368, DRAG_high=0.001225 m⁻¹ | [The Physics of Roller Coasters — World of Physics](https://world-of-physics.com/blog/physics-of-roller-coasters-loops-drops-g-forces/); [The Physics of Roller Coasters — UAF](http://ffden-2.phys.uaf.edu/webproj/212_spring_2015/Zachary_Milbradt/zachary_milbradt_212/drag_force.html); [Weight of a Train — CoasterBuzz](https://coasterbuzz.com/Forums/Topic/weight-of-a-train); [GRAVITY ROLLER COASTER LAUNCH — iepark.com](https://www.iepark.com/wp-content/uploads/2019/11/ROLLER-COASTER.pdf) | first-principles physical derivation (½ρC_dA/m), not the GEOMETRY/G-FORCE laws | **UPDATE 2026-07-21 (0.9x-of-record law)**: user directive — FRICTION and DRAG must each be 0.9x the MOST-ADVANCED (lowest-loss) real value; mid-envelope is too high for our slim train shape. Record-best specific drag = envelope low bound with best documented params (C_d 0.8, A 3 m2, laden m 9500 kg) = 0.000155 /m; **x0.9 -> `DRAG=0.00014f`** (now in code). Prior verdict (audit trail): 0.00040 falls centrally inside the physically-derived envelope (0.000155-0.001225 m⁻¹), close to the mid-estimate. Falcon's Flight 158 m near-vertical drop coast-down check (§ below) gives a 3.1% aero loss, a realistic order of magnitude for a near-freefall element, corroborating rather than motivating a change. No documented coefficient argues for a different number. **Secondary flag**: the in-code justification comment cites the 240 km/h speed target as the calibration rationale — this text should be replaced with the ½ρC_dA/m envelope + Falcon's Flight coast-down check, not because the value is wrong, but so a future reader isn't invited to re-tune DRAG against a speed target |
| `FRICTION` (rolling resistance) | `src/ride_constants.h:11 FRICTION=0.015f`, applied as a raw m/s² deceleration (not multiplied by GRAV anywhere) | Published/derived coefficients for coaster-grade polyurethane wheels on steel rail cluster at C_rr≈0.010-0.02 ("Rolling friction from polyurethane-coated wheels on steel rails is the most consistent friction source, with a coefficient of rolling friction around 0.01 to 0.02"); midpoint C_rr≈0.0125 gives physically correct deceleration `a_roll=C_rr·g≈0.0125×9.81≈0.123 m/s²` for a near-level section | [Rolling resistance — Wikipedia](https://en.wikipedia.org/wiki/Rolling_resistance); [Roller coaster wheel assembly — Wikipedia](https://en.wikipedia.org/wiki/Roller_coaster_wheel_assembly); [Coasters-101: Roller Coaster Wheel Design — Coaster101](https://www.coaster101.com/2011/10/24/coasters-101-wheel-design/); [Roller Coaster Wheel Material Science — Coaster101](https://www.coaster101.com/2016/04/13/roller-coaster-wheel-material-science-kate-kujawa/); [Polyurethane Wheels — Incredible Polyurethane](https://incrediblepolyurethane.com/polyurethane-wheels-help-give-roller-coasters-their-wild-ride/) | first-principles physical derivation (C_rr·g) | TOO_LOW. Current 0.015 is being used as if it were already a `C_rr·g`-style deceleration in m/s², but 0.015 is a bare C_rr-scale number — if `·g` was meant but omitted, the effective deceleration is ~8x weaker than 0.010-0.02 real C_rr implies; if 0.015 was meant to directly be an m/s² deceleration by design fiat, it is simply uncalibrated against any cited coefficient. Either reading points the same direction. **RESOLVED 2026-07-20**: raised to 0.10 m/s² (= record-low C_rr 0.010 x g, commit 706062c). **UPDATE 2026-07-21 (0.9x-of-record law)**: user directive — 0.9x the record-best value: **`FRICTION = 0.9 x 0.010 x 9.81 = 0.088f`** (now in code). Falcon's Flight coast-down check (158 m near-90° drop, where normal force and hence rolling resistance is ~0) shows FRICTION barely matters on that specific element either way (194.2 vs 193.0 km/h exit speed) — its effect is concentrated in slower/level sections (helixes, valleys, brake runs). Estimated lap-average impact of the fix: ≈-3.5 km/h / ≈-1.4% (245→242 km/h blended lap average on a synthetic 92-127s lap profile) — too small to move the existing 230-240 km/h design band or the [105,135]s lap-time pacing gate (`main.cpp:1103`) |
| Falcon's Flight coast-down cross-check | Numerically integrated `coastAcceleration` at 120 Hz down a 158 m near-90° drop from rest | Falcon's Flight top speed 250 km/h (69.4 m/s) reached at the end of the third of three LSM launches, which then carries the train up and over a 165 m camelback; first drop 158 m, "designed to simulate a near-freefall experience," descending at ~90° into a tunnel/valley, aided by additional LSM track in the valley itself — i.e. **250 km/h is launch-assisted, NOT a pure gravity coast-down result** (a frictionless free-fall from rest over 158 m tops out at only √(2·9.81·158)≈55.7 m/s≈200 km/h, 50 km/h short of the published top speed, consistent with LSM assist inside the drop) | [Falcons Flight — Intamin project page](https://www.intamin.com/project/falcons-flight/); [Falcons Flight — Wikipedia](https://en.wikipedia.org/wiki/Falcons_Flight); [Falcon's Flight — Coasterpedia](https://coasterpedia.net/wiki/Falcon's_Flight_(Six_Flags_Qiddiya_City)); [Specs Revealed — Theme Park Insider](https://www.themeparkinsider.com/flume/202101/7946/); [Six Flags Qiddiya City — official](https://sixflagsqiddiycity.com/en/explore/rides/falcons-flight) | derivation cross-check, not a project constant itself | INTENTIONAL_DEVIATION noted / OK as methodology — the memo explicitly corrects the framing that 158 m drop -> 250 km/h is NOT directly usable to validate DRAG/FRICTION together (LSM-assisted); the drop IS usable in isolation to sanity-check DRAG alone (normal force ~0 on a ~90° drop, so drag is the only loss term that matters). Coast-down table: frictionless bound 200.4 km/h; current constants (DRAG 0.00040, FRICTION 0.015) -> 194.2 km/h (3.1% loss); recommended constants (DRAG 0.00040, FRICTION 0.12) -> 193.0 km/h (3.7% loss); recommended with friction correctly zeroed on a 90° drop -> 194.2 km/h (3.1% loss, matching current). A 3-4% aero loss on a near-vertical high-speed drop is a physically reasonable order of magnitude, corroborating DRAG=0.00040 |
| Internal-consistency reproduction check | in-repo comment `ride_constants.h:57-59` claims "a flat 360 km/h arc reaches roughly 42 m/s at 2.1 km" under current constants | n/a — internal harness-fidelity check, not a real-world citation | n/a | n/a | OK — reproducing the exact scenario (100 m/s coasting on level track down to the 42 m/s `MIN_V` floor) with the memo's own simulation harness gives 2,146 m, matching the comment and confirming the harness used for the physics review reproduces the game's own physics |

**Caveats carried forward from the source memo:** laden train mass is not published by any
manufacturer for a record-class train — the DRAG envelope is bounded by scaling public per-seat
mass figures from *other* trains, not a Falcon's-Flight-specific number. `FRICTION` is applied
uniformly regardless of banking/grade (no `cosθ` factor) — a modeling simplification already
present and out of scope to change, which makes the recommended 0.12 m/s² a reasonable single
average rather than an exact per-section value. Falcon's Flight's headline 250 km/h is
launch-assisted and cannot itself validate DRAG/FRICTION in isolation — the 158 m first-drop
coast-down is the cleaner gravity-only reference point. Reproducibility artifacts referenced by the
source memo: `/tmp/claude-0/-home-user-Vibe-Coaster/6133a740-2d6c-536a-ae69-bf585476bdc3/scratchpad/coastdown.py`
and `.../scratchpad/lap_profile.py` (scratch-space scripts, not part of the repo).

---

## 8. SPEEDS & PACING

Source: `phase7_design.md` §0 and the turns/airtime audit's LAUNCH/BOOST/CHAIN rows (also listed
in §5 above; repeated here with the Phase-7 cliff-dive-specific figures for completeness).

| Parameter | Our constant (file:symbol, value) | Real-world basis (value, coaster, park, year) | Source (name + URL) | Law applied | Status/flags |
|---|---|---|---|---|---|
| Cliff-dive top speed at base | Derived: release ≈22 m/s (chain crawl speed) after a 160 m plunge -> `v≈√(22²+2·9.81·160)≈60 m/s≈217 km/h` (`phase7_design.md` §1.4) | Falcon's Flight top speed 250 km/h, reached via three LSM launches, not a pure gravity result | Same Falcon's Flight sources as §7 above | GEOMETRY LAW (drop scale), physics-derived speed (not record-matched by design) | INTENTIONAL_DEVIATION — explicitly "scaled-realistic vs Falcon's 250 km/h," i.e. the in-repo cliff-dive is deliberately gravity-only (no LSM assist modeled inside the plunge), so 217 km/h at the base is expected to be lower than the real ride's LSM-assisted 250 km/h peak. This is a design choice, not an error |
| Cliff-dive crest-hold duration | `CLIFFDIVE_CREST_HOLD_SECS=3.5f` (new constant, `phase7_design.md` §1.1) | B&M dive-machine holding-brake mechanic pauses the train 3-5 s (≈3 s typical) at the crest via a short chain-driven brake, then releases into a near-vertical (~90°) drop — cited generically to "B&M dive machines / Yukon Striker" (Canada's Wonderland, Vaughan ON, opened May 2019) | `phase7_design.md` §0 (no specific URL cited in the design doc; Yukon Striker's holding-brake mechanic is documented on [Yukon Striker — Wikipedia](https://en.wikipedia.org/wiki/Yukon_Striker)) | GEOMETRY LAW (mechanic-scale reference, not a drop/height record) | OK — 3.5 s sits inside the cited 3-5 s real range. Implementation note: true zero-speed hold is not expressible in the current propulsion model; the crawl is implemented as a 22 m/s creep (`CHAIN_V`) over ≈77 m of near-level crest track, timed to read as a 3.5 s hold — an explicit, documented approximation of the real holding-brake mechanic, not a literal brake element |
| Cliff-dive reference drop | `CLIFFDIVE_REFERENCE_DROP=160.0f` (new constant, `phase7_design.md` §1.1) — "Falcon's Flight 525 ft vertical cliff drop" | 195 m cliff, ~160 m (525 ft) vertical drop along the cliff face into a tunnel — Falcon's Flight, Six Flags Qiddiya City, Intamin; verified via WebSearch 2026-07 per the design doc | Falcon's Flight sources as cited in §4/§7 above | GEOMETRY LAW (record == reference, 0.75-1.5x scale window, `CLIFFDIVE_MIN_DROP=120.0f`/`CLIFFDIVE_DROP_CAP=240.0f`) | MATCHES_RECORD (design-stage; not yet built/audited — this is a Phase-7 design spec, not yet verified against a built census). Flagged for issue #10's future 3D geometry audit once implemented |
| Cliff-dive pull-out g target | `Gt≈7.0-8.0`, `r=v²/((Gt-1)·GRAV)` (`phase7_design.md` §1.3) | ~3.5-4 g sustained — real dive-machine pull-outs, generic citation (no single named ride in the design doc) | `phase7_design.md` §1.3 (no specific URL) | G-FORCE LAW (2x-record) | OK per the law as stated (2x of 3.5-4 g = 7-8 g), but the underlying 3.5-4 g real-record figure is **uncited to a specific ride** in the source design doc — same posture as several rows in §3 (corkscrew, Immelmann, dive loop) that cite "project-cited real" figures without an external source |
| Speed push target (average) | `--forceaudit` gate `aggregateKmh∈[230,255]` (target ~240), `main.cpp:861-879`, up from a measured baseline of ~215 km/h | Not record-derived — an internal project pacing gate. Design doc explicitly states the user **forbids artificial speed pushing** ("do not raise the troughs to hit a number"); any constants change must be justified forward from real-world physics (see §7 DRAG/FRICTION), never backward from the 240 gate | `phase7_design.md` §2.3 (project directive, not an external source) | PROJECT_CHOICE (pacing gate, not record-derived) | PROJECT_CHOICE — organic levers (altitude discipline, terrain-sited drops, less time parked slow) are primary; physics-constant changes are only legitimate if independently justified by real-world coefficients (§7), and if organic levers land the average below 230, the design doc requires reporting the real number and discussing the gate, not quietly re-tuning constants to close the gap |
| Yukon Striker crest-hold citation | (mechanic reference only, see crest-hold row above) | Yukon Striker, Canada's Wonderland (Vaughan, ON), B&M dive coaster, opened May 2019 | [Yukon Striker — Wikipedia](https://en.wikipedia.org/wiki/Yukon_Striker) | GEOMETRY LAW (mechanic reference) | UNVERIFIED_SOURCE for the specific "3-5 s" duration figure — the design doc states this as a known B&M dive-machine characteristic without a pinned citation; flagged for follow-up sourcing |
| **Escarpment caprock drop-fraction** (§0.95(b), phase7b terrain) | `CAP_FRAC = 0.65` (dimensionless), `src/environment.cpp` `tuwaiqEscarpment`; also `phase7_design.md` §0.95(b). Sets the near-vertical caprock band = `0.65 × rimRelief`; talus carries the remaining `0.35`. Measured on the tall rim: caprock 159 m of a 245 m drop = **0.65 exactly** (`--cliffsites` BEST CLIFF-DIVE line) | **Fraction of total drop held at true vertical (≈90°) on 90°-class record drops.** Yukon Striker: 75 m (245 ft) drop, riders "hang over the 90° drop … then plunge straight down the 75 m," held vertical for *most of its height*; Kingda Ka: 127 m (418 ft) 90° tower drop that is near-pure vertical, curving only into the bottom 270° spiral (total tower 139 m → drop is 91% of the tower, i.e. the vertical dominates). The remainder of any such drop is consumed by the top crest-over transition (small on a near-rest B&M holding-brake release) and the bottom pull-out arc. Representative fraction across the 90°-class = **0.65** (mid of the documented ~0.55–0.70 "most of its height" band) | [Yukon Striker — Wikipedia](https://en.wikipedia.org/wiki/Yukon_Striker); [Kingda Ka — Wikipedia](https://en.wikipedia.org/wiki/Kingda_Ka); [Yukon Striker — CNN Travel](https://www.cnn.com/travel/article/yukon-striker-roller-coaster-opens-canada/index.html) | **RATIO LAW** — dimensionless fraction of the real ride's *own* drop, re-applied to OUR sited drop (120–240 m): sustained-90° caprock length = `0.65 × siteDrop` = 78 m (1.0× floor) … 156 m (1.5×), ≥130 m at the ~1.25×-mean (200 m). Never an absolute-metre transplant | OK — matches spec §0.95(b) target "~130–150 m of 85–90° caprock on tall stretches"; probe measures 159 m on the 245 m rim (0.65 of drop). The 0.65 mid-band figure is an engineering representative, not a single-ride Guinness datum; the ~0.55–0.70 range is stated by design anchor, the endpoints not pinned to a per-ride vertical-section measurement (flagged like the crest-hold rows) |
| Escarpment caprock face angle | `TAN_CAP = tan(86°)`, `src/environment.cpp` `tuwaiqEscarpment` | Real mesa caprock is a sheer near-vertical cliff (≈90°). Spec §0.95(b) band = **85–90°** | RATIO LAW — `86° = 0.956 × 90°` (of the true-vertical cap); 86° chosen so the 3.5 m-sampled height-field cleanly resolves ≥85° per-sample (a full 90° collapses the caprock into a single column) while staying solidly in-band | OK — probe reads steepest face 85.6° and caprock band ≥85°; within the 85–90° spec |
| Escarpment talus face angle | `TAN_TAL = tan(65°)`, `src/environment.cpp` `tuwaiqEscarpment` | Structural talus/run-out apron below the caprock. Spec §0.95(b) band = **60–70°** | RATIO LAW — `65° = 0.72 × 90°`; mid of the 60–70° spec band | OK — note this is the escarpment's steep *structural* apron per spec, not a literal scree angle-of-repose (~34°); the spec fixes the 60–70° band |
| Graduated-biome height bands | `terrainH` multi-knot relief spline (`splineEval`, knots `kRT`/`kRV`), `src/environment.cpp`; §0.95(a). Bands measured (`--terrainaudit` histogram): plains 18–80 m ≈83%, rolling 80–120 m ≈8%, mountain 120–200 m ≈5%, badlands mesa 200–280 m ≈2% of land | Not coaster-derived. **Minecraft 1.18 terrain-shaper** structure — three large-scale fields (continentalness/erosion/peaks-valleys) drive a nested multi-point spline mapping to a continuous elevation, so biome height is populated across a graduated range rather than bimodally. Used as OPEN NUMERIC REFERENCE only (wiki spline tables; cubiomes is MIT) — concepts/structure, no GPL/NC code copied | [Minecraft Wiki — World generation / Noise settings](https://minecraft.wiki/w/World_generation); [cubiomes (MIT)](https://github.com/Cubitect/cubiomes) | PROJECT_CHOICE (composition target, MC-referenced) — like the waterfrac 10–15% row, an internal graduated-occupancy target, not a real-park measurement. Knot heights are expressed in OUR height envelope (WATER_Y=18 … TERRA_MAX=280), not transplanted from MC's y∈[-64,320]. Gate: rolling ≥4% AND mountain ≥4% of land, no near-empty bin in the 80–200 m approach span | OK — `--terrainaudit` reports GRADUATED (rolling 8.1%, mountain 4.4%, min approach bin 0.60%); no bimodal gap |

---

## 9. DURATIONS

See §5's `REAL_ELEMENT_SECONDS` row for the full per-element duration table (TURN 5.5s, HILLS
5.0s/lobe, SCURVE 5.0s, DIP 4.0s, BANKAIR 4.0s, WAVE 5.0s) — all self-labeled in-code as
"clean-room estimates," not sourced real-world records, and consumed only as a soft bias, never a
hard reject. Repeated here for the DURATIONS domain per the requested table structure:

| Parameter | Our constant (file:symbol, value) | Real-world basis | Source | Law applied | Status/flags |
|---|---|---|---|---|---|
| `REAL_ELEMENT_SECONDS[M_TURN]` | `gen_constants.h:278-298` = 5.5f | No external POV-timing citation exists; internally consistent with 210 m reference length @ 36-44 m/s (4.8-5.8 s) | none (self-labeled "clean-room estimate," `gen_constants.h:273-277`) | project pacing design | OK (internally consistent) |
| `REAL_ELEMENT_SECONDS[M_HILLS]` | 5.0f (per lobe) | 233.5 m rail @ 48-66.85 m/s (`HILL_ENTRY_MIN/MAX`) implies 3.5-4.9 s | none | project pacing design | OK, table runs slightly slower than geometry implies |
| `REAL_ELEMENT_SECONDS[M_SCURVE]` | 5.0f | 140 m plan @ Fury 325's cited 42.5 m/s (95 mph, real speed) implies 3.3 s, i.e. the table's 5.0s implies only ~28 m/s | [Fury 325 — Wikipedia](https://en.wikipedia.org/wiki/Fury_325) | project pacing design | **Outlier flag** — table is the slowest of the group relative to its one real-world speed anchor; worth a closer look (see §5 cross-cutting finding) |
| `REAL_ELEMENT_SECONDS[M_DIP]` | 4.0f | ~168 m computed forceLen @ genV=50 m/s implies 3.4 s | none | project pacing design | OK (internally consistent) |
| `REAL_ELEMENT_SECONDS[M_BANKAIR]` | 4.0f | 196 m @ 48-55 m/s implies 3.6-4.1 s | none | project pacing design | OK, tight match |
| `REAL_ELEMENT_SECONDS[M_WAVE]` | 5.0f | 253-288 m @ 50-55 m/s implies 4.6-5.2 s | none | project pacing design | OK, tight match |
| Lap duration pacing directive | `[105,135]` s lap-time pacing gate, `main.cpp:1103`; Phase-7 references a "120s lap directive" cadence | Not record-derived — an internal project pacing gate, cross-checked only against the code's own duration table and cadence figures, not an external ride's lap time | none (PROJECT_CHOICE) | PROJECT_CHOICE | PROJECT_CHOICE — see §10 for the full citation of this directive's derivation |

---

## 10. CLEARANCE / ENVELOPE

These rows are **project engineering choices**, not real-world-record derivations — the GEOMETRY
LAW and G-FORCE LAW do not apply to them. They are included for completeness and traceability, and
marked `PROJECT_CHOICE` rather than judged against either law. Each row cites the in-repo location
where the project-choice derivation is documented.

| Parameter | Our constant (file:symbol, value) | Basis | Source | Law applied | Status/flags |
|---|---|---|---|---|---|
| Occupancy / structural clearance envelope | `genc::OCCUPANCY_ENVELOPE=6.0f`, `gen_constants.h:195`; also `genc::SUPPORT_TRACK_CLEARANCE≈3.0f` and `genc::TREE_TRACK_CLEARANCE≈6.0f` (Phase-7 structure/scenery clearance additions, `phase7_design.md` §3Y) | ASTM-derived project constant — a train-envelope clearance figure, not a specific coaster's published dimension. `TERRAIN_DECK_CLEARANCE=2.0f` (`gen_constants.h:174`) is a related generic clearance constant | Not an external citation — an in-repo engineering derivation from ASTM amusement-ride clearance practice (referenced generically; no specific ASTM standard number or URL was cited in any of the six source audits reviewed for this consolidation) | PROJECT_CHOICE (ASTM-derived) | PROJECT_CHOICE — **UNCITED at the ASTM-standard level**: none of the six source audits trace `OCCUPANCY_ENVELOPE=6.0f` to a specific ASTM standard/section number or public document. Recorded here as a project-engineering constant, not a record; flagged for a future citation pass (candidate for issue #10's 3D geometry audit, since occupancy/clearance is fundamentally a 3D concern) |
| Cliff-dive face-setback / support-height bounds | `CLIFFDIVE_FACE_SETBACK_MIN=4.0f` (= `OCCUPANCY_ENVELOPE_ESCAPE`), `CLIFFDIVE_FACE_SETBACK_MAX=12.0f`, `CLIFFDIVE_SUPPORT_H_MAX=22.0f` (new constants, `phase7_design.md` §1.1/§1.5) | Derived geometrically from the occupancy envelope and a "short cliff-face anchor, not a mega-tower" design intent (22 m ≈ a 6-7-storey face anchor with `legR≤0.55`, `baseHalf≤~4m`) — not a real-ride citation | `phase7_design.md` §1.5 (in-repo geometric derivation, no external source) | PROJECT_CHOICE (derived from the ASTM-derived occupancy envelope above, plus a stated design intent) | PROJECT_CHOICE — design-stage only (not yet built/audited); the 12 m upper setback bound and 22 m support-height cap are justified by supports-must-be-short reasoning in the design doc, not by an external record |
| Water fraction directive | 10-15% water coverage target, gated via `--waterfrac` probe (Phase-3 state measured 12.9%, `phase7_design.md` §0.9/§3.1) | Not real-world-derived — an internal project visual/composition directive | `phase7_design.md` §3.1 (`--waterfrac` gate); no external citation | PROJECT_CHOICE | PROJECT_CHOICE — purely an internal composition target; the 10-15% band is a project design choice about how much of the generated terrain should read as water, not a measurement of any real park's water coverage |
| Lap duration / cadence directive | `[105,135]` s per-lap pacing gate (`main.cpp:1103`); `nominalCadence(1700 m)` boost cadence (`ride_constants.h`, referenced `phase7_design.md` §0.1/§2.2); `cadenceMean∈[1.75,2.35]` km gate (`main.cpp:861-879`); Phase-7 references a "120s lap directive" as the target cadence within that band | Not real-world-derived — an internal project pacing/duration directive, informed only loosely by real coasters' typical ride-operation durations (no specific real coaster's lap/cycle time is cited as the source of the 105-135s band in any of the six source audits) | `main.cpp:1103` (gate definition); `phase7_design.md` §0.1, §2.1-2.5 (cadence/pacing discussion) | PROJECT_CHOICE | PROJECT_CHOICE — the 105-135s band and 1700 m nominal boost cadence are project pacing decisions, cross-checked internally against `REAL_ELEMENT_SECONDS` (see §9) but not derived from any single real ride's cycle time. Note the coupled risk documented in `phase7_design.md` §2.4: raising trough speeds to hit the average-speed target shifts entry-speed distributions in ways that can push HELIX/HILLS shares out of band — this is a project-internal tuning risk, not a real-world-citation issue |

---

## 11. Changelog

| Date | Change | Author/source |
|---|---|---|
| 2026-07-20 | Initial consolidation of six source audits (`gforce_2xwr_audit.md`, `wr_baseline_audit_inversions.md`, `wr_baseline_audit_turns_airtime.md`, `share_targets_research.md`, `physics_realism_memo.md`, `phase7_design.md` §0) into this single traceable reference document. No repo constants were changed by this consolidation — all `PENDING-FIX` rows above reflect the *source audits'* recommendations, not yet applied to code. | consolidation pass |
| 2026-07-21 | STALL micro-height floor: added `stallHFloor = GRAV·3.0²/8 ≈ 11.04 m` (freefall float-time law, T=3.0 s mid-band of the real 2-4 s zero-g float range, ArieForce One's "nearly four seconds" cited) to `initStall`'s post-refit `stallH`, replacing the missing floor that let hot 51-75 m/s entries collapse the crest to H=2.8 m. Verified via temporary `MC_STALLTRACE` env-gated probe then removed; `--census 4` shows STALL alive (n=8, 3.1% share), deadSubtype=0, complete=yes | Fable takeover, micro-STALL floor task |

**Instruction for future maintainers:** any future change to a constant listed in this document
(value, citation, scaling law, or status flag) MUST update the corresponding row here in the same
change — including updating the Status/flags column if a `PENDING-FIX`, `TOO_HIGH`, `TOO_LOW`, or
`UNVERIFIED_SOURCE` flag is resolved, and adding a new row to the Changelog table above with the
date and a one-line description of what changed and why. If a cited real-world record is
superseded (as Formula Rossa's top-speed record was by Falcon's Flight on 2025-12-31, and as
ArieForce One's operation ended 2026-08-02), update the affected row's "date-context of the claim"
language rather than silently replacing the old figure — the audit trail of *which* record was
believed true *when* is part of what makes this document auditable.

## Addendum 2026-07-21 (phase7a takeover, commit f8ad2b5) — new/changed parameter rows

| Parameter | Code | Real-world anchor + derivation | Verdict |
|---|---|---|---|
| ROLL (corkscrew) stretch law | `initRoll`, `v1/coaster_track.cpp` | Rider-frame roll PERIOD pinned to `REAL_ELEMENT_SECONDS[M_ROLL]`=2.8 s/rev (Arrow corkscrew-era timing); axis advance per rev scales with entry speed (the geometry of real high-speed rolls, e.g. VelociCoaster's high-speed roll, Steel Vengeance's rolls — rotation time stays human, element stretches with speed). Steady radial load rides as VERTICAL g (banked corkscrew) ≈9 g at scale 1.05; roll-in/out lateral transient ∝ radius ⇒ radius held to **[1.0,1.05]x of the 6.6 m Arrow reference** (measured: 1.30x → lat 6.4 g breach; 1.05x → ~5.2 g inside the 6 g envelope) | INTENTIONAL_DEVIATION from the [1.0,1.5]x spectrum law — physics-bound, documented in-code with measurements |
| STALL window | `initStall` + `invVMax(M_STALL)` 56→75 | Zero-g stall is speed-agnostic by construction (quartic span L=4·sqrt(h·vc2/G) cancels gravity at crest for any entry speed); window opened to the ride's operational top. H clamp lower bound under review (H=2.8 m micro-stall observed once) | OK (speed-agnostic derivation in-code); H floor PENDING-REVIEW |
| STALL micro-height floor | `initStall`, `v1/coaster_track.cpp` (`stallHFloor`) | The length-capped-span refit (`stallH = GRAV·Lf²/(16·vc2·1.32)`, span capped ≤12 segments = 168 m for footprint) scales stallH ~1/vc2, so hot 51-75 m/s entries collapsed the crest to a few meters (H=2.8 m measured once at v=74 m/s) — physically a true zero-g curvature match, but visually a non-existent "blip". Real zero-g stalls are marketed by FLOAT TIME, not curvature purity: ArieForce One (Fun Spot America Atlanta, opened April 2023, permanently closed Aug 2 2026 — see row above) is self-billed as delivering "nearly four seconds of floating airtime". During true zero-g the vehicle is in freefall, so crest-to-shoulder drop over a T-second float is the standard freefall relation h=g·(T/2)²=GRAV·T²/8. Floored at the representative mid-band T=3.0 s (below ArieForce's ~4 s marketed ceiling, above the 2 s "still reads as a stall" line): GRAV·9/8 ≈ 11.04 m. `maxClearH()`'s own hard floor is 6 m, so 11.04 m is always satisfiable across the whole 51-75 m/s window (verified: `MC_STALLTRACE` env-gated probe, 9 builds all genV 60-74 m/s, all Lf=168 m capped, all stallH=11.04 m=floor, all maxClearH 112-208 m; trace removed after verification) | RESOLVED — H floor no longer PENDING-REVIEW. `--census 4` post-fix: STALL n=8 alive (3.1% share), deadSubtype=0, complete=yes |
| LOOP entry window | `invVMax(M_LOOP)` 64→74.9 m/s | Loop sizing law height=2v²/(14g) makes crest felt-g scale-invariant (v_top² and crown radius both ∝ v²), so the safe window extends from the 1.0x record-height crossover (61 m/s) to the 1.5x crossover: v=sqrt(1.5·54.56·14·9.81/2)=74.9 | RESOLVED (was pinning every loop at the 1.0x floor, census ter 5/0/0) |
| TURN g-law sweeper | `initTurn` | Above the speed where the 9.6 g plan needs radius > 1.5x window cap, radius follows the g-law UNCAPPED (Formula Rossa-style post-launch sweepers; no built coaster corners at 300+ km/h). EXEMPT from recordScale/spectrum (physics-mandated routing, not a record element); yaw sanity still gated. Without the exemption the 75-100 m/s band had no eligible elements and laps collapsed to escape-budget force-launches (measured 21 s census mean) | INTENTIONAL_DEVIATION, load-bearing, measured |
| TOP HAT draw split | `beginTopHat` | Old hardcoded minor-hi 195 / major-lo 235 left 1.18-1.42x undrawable (census ter 4/0/5). Split now at the reference midpoint 1.25x=206.25 m, derived from `TOP_HAT_RECORD_RISE` | RESOLVED (row "TOP HAT scaling bounds" magic-number flag) |
| BANKAIR/WAVE height ceilings | `initBankAir`/`initWave` | Hardcoded 49/46 m (=1.4x/1.31x of the 35 m reference) amputated the upper tercile; now `BANKAIR_RECORD_HEIGHT*RECORD_SCALE_CAP`=52.5 m, still bounded by energy/terrain affordable | RESOLVED |
| BOOST pacing recalibration | `ride_constants.h` 292→278 km/h cruise, 1.7→2.1 km cadence | Same documented calibration loop as the prior hand-tune, re-run for the 0.9x-of-record loss law (old pacing pinned laps at 258-267 km/h, starving every entry window; new pacing decays coast arcs 77→~49 m/s through the hill/inversion windows; measured lap avg 246 km/h vs the 240 organic target) | OK, measured |
| Census spectrum/duration/height gates | `src/main.cpp` census block | User size-spectrum law 2026-07-21: capViol must be 0; no one-signature elements among n≥6 (terciles of the [1.0,1.5] window); duration mean ~0.75x real with no element averaging >1.0x; 25 m height-bucket histogram ground→top-hat | Instrumentation (TxnSnapshot-safe ScaleStat in GenCursor) |

### Addendum 2026-07-21 (phase7a hands-on takeover, fable)
| Constant / law | Value | Derivation & source anchor |
|---|---|---|
| CUT_ENTER_CLEAR / CUT_TARGET_CLEAR / CUT_EXIT_LIMIT | 8 m / 10 m / 16 | Scheduler clearance thresholds for the first-class cut-exit lift-out (archetype-B desert fix). ENTER = measured buried-boundary band (clr −11..+3 m) plus the ~8 m a next element's entry needs; TARGET adds 2 m hysteresis (TERRAIN_DECK_CLEARANCE family); LIMIT bounds consecutive lift-outs. Not physical-record constants — routing thresholds derived from the measured defect. |
| IMMEL half-roll window | t ∈ [0.55, 1.00] of loop curve (was [0.45,0.95]) | Immelmann = half-loop + half-roll "near/through the top" (existing IMMEL_RECORD refs). In this builder pitch is vertical at t≈0.5 and crest (θ=π) at t≈0.9; rolling across the vertical-pitch zone produced authored-bank roll-accel 6.99–9.47 deg/m² vs the 5.5 gate (ROLL_ACCEL_MAX_DEG_PER_M2). Window measured to 5.43 (seed1) / 4.10 (seed2), gate-clean, signature intact (roll blends through crest, exit descending 24°). |
| Last-resort stub steering cap | total yaw ≤ MIN_CONN·SEG_LEN·2g·G/v² | Daylight-steered escape stub: candidate heading bounded by the curvature a 2 g uniform lateral allows at current speed; the connector's eased shoulder concentrates rate ≈2×, so felt peak ≈4 g — inside the |lat| ≤ 6 g law. Uncapped candidates measured 9–17 g single-span kinks. |
| straightEntryOK yaw-rate gate | \|genPrevDyaw\| ≤ clamp(2.4·SEG_LEN·G/v², 0.001, 0.24) | Same 2.4 jerk-law constant as the existing connector/settle limits (single source); gates every fixed-heading analytic macro entry (top hat, hill chain, straight/curved drop) whose frame math assumes a straight entry. Measured defect without it: 15–17° tangent step, 1.85–2.05 /m² curvature-jerk, 27.7 deg/m² roll-accel at drop joints. |
